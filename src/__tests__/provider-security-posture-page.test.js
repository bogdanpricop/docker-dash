'use strict';
const page = require('../../public/js/pages/provider-security-posture');
describe('provider security posture page', () => { it('labels coverage as declared evidence', () => { const html = page._coverageHtml({ coverage: { declaredFeatureCount: 3, states: { supported: 1, conditional: 1, unsupported: 1 } } }); expect(html).toContain('Declared SDK contract evidence'); expect(html).toContain('unsupported'); }); it('does not imply that declared safeguards execute an operation', () => { const html = page._safeguardsHtml({ safeguards: { declaredPrivilegedFeatureCount: 2, approvalRequired: 1 } }); expect(html).toContain('no operation is attempted'); expect(html).toContain('Four-eyes'); }); it('labels console data as declared safeguards', () => { global.Utils = { escapeHtml: value => String(value) }; expect(page._consoleHtml({ consoleExposure: { state: 'conditional', singleUseToken: true } })).toContain('not proof of a live console'); }); });
