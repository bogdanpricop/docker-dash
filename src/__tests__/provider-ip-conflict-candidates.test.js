'use strict';
jest.mock('../services/provider-sdk/ip-address-inventory', () => ({ inventoryForHost: jest.fn() }));
const source = require('../services/provider-sdk/ip-address-inventory'); const candidates = require('../services/provider-sdk/ip-conflict-candidates');
describe('IP conflict candidates', () => { it('reports only repeated observations as candidates', async () => { source.inventoryForHost.mockResolvedValue({ provider: { type: 'xen' }, observedAt: 'now', coverage: { complete: true }, summary: { addressCount: 3 }, addresses: [{ address: '10.0.0.1', family: 'ipv4', vm: { id: 'a' }, nic: { id: 'n1' }, source: 'provider', connected: true }, { address: '10.0.0.1', family: 'ipv4', vm: { id: 'b' }, nic: { id: 'n2' }, source: 'provider', connected: true }, { address: '10.0.0.2', family: 'ipv4', vm: { id: 'c' }, nic: { id: 'n3' }, source: 'provider', connected: true }] }); const result = await candidates.candidatesForHost({ id: 7 }); expect(result.summary.candidateCount).toBe(1); expect(result.candidates[0]).toEqual(expect.objectContaining({ address: '10.0.0.1', observationCount: 2 })); expect(result.limitations.join(' ')).toContain('candidate only'); }); });
