# Imaginea verificata 8.96.8

Imagine: `docker-dash:8.96.8-audit-29c26d2`.
Commit: `29c26d2b248cbc57c580ed8df57b1d9744123064`.
ID: `sha256:1085b24aa2d0531faa15b62ae32b3ef93f92532a47d237ef978654464570741b`.

Checkpoint-ul include [durata cotelor HTTP si traversarea routerelor](2026-09-20-quota-lifecycle.md)
si [coordonarea curatarii cu recuperarile Docker](2026-09-20-prune-recovery.md).
Sursele au trecut 362 suite / 4762 teste, unul omis; lint si help 60/60 trecute.
Verificarea npm din acest checkpoint: zero vulnerabilitati, zero dependente
directe outdated. Pachetele OS si binarele Go sunt evaluate separat.

Pe fiecare host, imaginea exacta a trecut probe Compose si aplicatie
Linux: pornire, SQLite, restart, chei persistente, Git/OpenSSH, TLS provider, LDAP
si health independent de cota API. Alte probe acopera 10 scenarii HA si 12
HTTP/Redis, cu hash-urile surselor din imagine verificate inainte de rulare.
Doar fixture-urile sunt adaugate controllerelor; serviciile testate nu sunt
suprascrise. Redis 8.10.1 este numai backend-ul temporar fixat al probelor.

Proba prune a verificat hash-urile a cinci servicii din imagine. Orchestrarea
foloseste modulul local identic, prin API Docker real, cu nume izolate si filtre
pozitive limitate la resursele proprii. Cele cinci scenarii verifica rezervarea,
excluderea la container prune, pastrarea helper-ului la image prune, blocarea
pentru recuperari si retinerea rezervarii dupa un rezultat incert simulat.
Nu se executa prune pe resursele de productie. Curatarea fixture-ului foloseste
`noprune=true` si verifica pastrarea imaginii helper de baza.

Trivy: 4 High, 2 Medium, 3 Unknown. Grype: 4 High, 5 Medium, fara Critical.
Comparatia cu 8.96.7 confirma aceleasi ID-uri, pachete, versiuni instalate si
severitati. Manifestul OCI, configuratia si ordinea celor 20 de straturi sunt
verificate pentru ambele scanari. Constatarile raman deschise.

Rezultatele brute, identitatile OCI si hash-urile sunt in
[raportul JSON](2026-09-20-image-8.96.8.json). Validarea de deploy este in
[raportul separat](2026-09-20-deployment-8.96.8.md). Imaginea este transferata
privat; pragurile scannerelor si ale admiterii raman active. Nu se adauga exceptii
pentru constatarile ramase si nu se pretinde securitate completa.
