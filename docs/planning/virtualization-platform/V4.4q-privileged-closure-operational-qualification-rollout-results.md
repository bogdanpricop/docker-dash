# V4.4q — Privileged closure operational qualification rollout results

## Scope

Release v8.90.0 qualifies exactly B167–B176. B167–B168 were implemented in
v8.83.0, B169–B175 in v8.84.0, and the already `Done` B176 permission-catalog
foundation in v8.79.0. B176 is revalidated, not claimed as a new feature.

The release reads local security, privileged-access and compliance evidence
without starting a provider mutation, network call, notification, temporary
account, media recorder or external command. Every external adapter, canary and
browser gap remains explicit.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,352 tests passed and 4 were skipped (3,356 total);
- 5 focused suites and 39 focused tests passed;
- the existing egress-policy shared-file test passed 47/47 in isolation, and
  the complete deterministic serial rerun passed after one parallel local run
  observed cross-suite allowlist interference;
- ESLint completed with zero errors and 14 pre-existing warnings;
- registry validation remained 450 features: 391 Done, 59 Partial, 0 Open;
- i18n, accessibility, syntax and whitespace gates passed;
- the integrated browser runtime exposed no browser, so smoke remains
  explicitly `not_recorded`.

GitHub:

- feature commit `b0ada59810bb4c2efd6739a98b55be65e8ab1697`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.90.0`;
- CI run `30674346451`, tag Docker run `30674346450` and PR Docker run
  `30674346477` all succeeded;
- draft PR `https://github.com/bogdanpricop/docker-dash/pull/13` tracks the
  release branch.

## Deployment evidence

The exact tag archive contained 1,894 entries, was 20,141,403 bytes and had
SHA-256
`c1e89a39b59c257ab98fdb98a956b268b8f79d27022f4e8d62ccae6e7fa21ddc`.
It excluded `.env`, `.git/` and `node_modules/`; both transfers matched the
digest and extraction preserved each `.env` digest, owner and mode.

The application was quiesced only while copying SQLite. The first intermediate
restart used the legacy `APP_VERSION` pinned in each retained `.env` and briefly
selected the local v8.82.0 image. Image inspection detected this immediately;
both targets were explicitly restored to their exact v8.89.0 image, healthy
with zero restarts, before backup-integrity verification, extraction and the
v8.90.0 build. No data or configuration was changed by the corrected restart.

Retained rollback copies:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.90.0-b0ada59.db` | 2,107,269,120 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.90.0-b0ada59.db` | 454,377,472 | 0600 | `ok` |

Each target built the Dockerfile `production` target as
`docker-dash:8.90.0` from the verified archive, and only Compose service `app`
was force-recreated.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:6863bb513d878b0664f4af56065253b9af3c24a178053f0296417373ac365895` | `8.90.0`, healthy | 0 |
| VPS | `sha256:177ed11f7abb0c790fb73a9cc4da852553abd0402c7966b75c9d884b68200cba` | `8.90.0`, healthy | 0 |

No migration is introduced. The latest applied migration remains
`169_provider_privileged_compliance.js` (170 recorded rows on LAN and 169 on
VPS); live and backup `PRAGMA integrity_check` return `ok`.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Enabled flags | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 (`ESXi-6`) | 10/10 | 1 | 2 | 0 | `e424ec2bfa4661d179ed71dc03e4244b64df270ea029745590dfc7a304672ccf` |
| VPS | synthetic Proxmox host 1; no active provider exists | 10/10 | 1 | 2 | 0 | `6827596adf1fb35a2bf5184982c247658f6b90a3ee853a22fc47e821bf5ea9b5` |

Both envelopes contain exact ordered IDs B167–B176, schema version 1.5, mixed
implementation releases and stable hashes across repeated reads. Both targets
retain `DD_PROVIDER_SECURITY_LIFECYCLE=true` and
`DD_PROVIDER_PRIVILEGED_COMPLIANCE=true`; low-risk remediation remains off.
Flags are not interpreted as evidence: B167–B175 remain `not_observed` with
zero scoped records, while B176 is observed only because all ten exact
privileged/compliance permission keys exist.

All five earlier batches retain their exact IDs, 10/10 schema, stable hashes
and zero safety counters. On both targets the protected endpoint returns 401
without a session, runtime logs contain zero error/fatal/uncaught entries, and
internal plus workstation health report v8.90.0 with zero restarts.

Temporary release archives were removed locally and remotely. Both rollback
database backups were intentionally preserved.
