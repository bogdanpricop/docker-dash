# V5.6c / V6.1a — Unified Kubernetes platform rollout

**Release:** v8.64.0

**Feature commit:** `d7f4b0b`

**Date:** 2026-07-29

## Outcome

B316–B325 were pushed to `agent/advanced-compose-gitops`, included in PR #13,
tagged `v8.64.0` and deployed as `docker-dash:8.64.0-d7f4b0b` to LAN and VPS.
Both app containers are healthy with zero restarts and five mounts. No optional
Compose profile or sidecar was recreated.

## Verification before publish

- Full Jest: 274/274 suites passed; 3,003 tests passed and 4 skipped (3,007
  total).
- Focused B316–B325 suite: 11/11 tests passed. The wider Kubernetes and
  governance selection passed 53/53.
- ESLint: 0 errors and the same 14 historical warnings.
- Syntax checks passed for the client, service, routes, browser API/page,
  migration and What's New source; `git diff --check` passed.
- GitHub Actions CI run `30496199129` and Docker build runs `30496199109` and
  `30496199476` are successful. PR #13 reports a clean merge state.

## Artifact

- Exact Git archive: `docker-dash-8.64.0-d7f4b0b.tar.gz`.
- Size: 19,508,950 bytes.
- SHA-256: `1e6eb864aa7157c8bc50a85e4931e5e9564180e68e69c47fde2c8f99e295cc89`.
- The checksum matched after transfer on both hosts. Exact incoming archives
  were removed after successful verification; extracted releases remain.

## Backup and deployment evidence

| Target | Runtime | Verified online backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.64.0-d7f4b0b`, image ID `sha256:2cc89f91bd2180cde04dc443f78a20611bf585a0dc74a4bd503b3096fcbc9c49`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v56c-v61a-20260729T223019Z-d7f4b0b/docker-dash.db`, 3,759,169,536 bytes, mode 600, SHA-256 `d6ab3aaed5cf1f65de15899282093595ef85b1b280c76f9184ea70eac9e0176a` |
| VPS `89.37.212.66` | image `docker-dash:8.64.0-d7f4b0b`, image ID `sha256:cd7497a9a1517ce1abf2fbe7d9dc3961a78fb34620e55e55c86d473be8246f94`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v56c-v61a-20260729T223019Z-d7f4b0b/docker-dash.db`, 440,188,928 bytes, mode 600, SHA-256 `f81bab0adad5394ec1b9c1015102b75b307679f4cae4cca88a1724e9dc4d5fb5` |

Each backup was produced with the running application's `better-sqlite3`
online backup API. Before build/recreate, the 8.63.0 image opened the exact
backup file read-only through a direct file mount under a writable `/backup`
tmpfs, with no network and a read-only root filesystem. `quick_check` returned
`ok`; the previous image observed 280 tables.

The first rollout invocation stopped before backup, build or recreation because
the release-version precheck incorrectly assumed Node was installed on the
host. The running 8.63.0 containers remained healthy and unchanged. The retry
used a host-native bounded file check, then completed the full safety sequence.

## Migration and smoke verification

- Migration `138_unified_kubernetes_platform.js` ran exactly once per target.
- All nine exact new tables exist. Both databases contain 57 governance
  permissions, five admission policies and five curated provisioning workflows.
- Post-deploy SQLite `quick_check` returned `ok` on both targets.
- Internal `/api/health` reports version `8.64.0` and standalone mode.
- External `/` and `/api/health` return HTTP 200 on LAN and VPS. The new catalog
  and lifecycle evidence routes return HTTP 401 without authentication.
- The production build uses target `production`; `npm ci --omit=dev` still
  reports six known advisories (one low, two moderate and three high).
- Existing environment warnings remain unchanged: LAN has CSRF disabled and a
  paused Romarg SSH credential; runtime cookie/audit-retention posture was not
  modified by this feature batch.

## Rollback readiness

The previous image `docker-dash:8.63.0-61c46c3` and extracted release
`v8.63.0-61c46c3` remain on both hosts. Current extracted releases are:

- LAN: `/home/localadmin-a/docker-dash-releases/8.64.0-d7f4b0b`;
- VPS: `/opt/docker-dash/releases/8.64.0-d7f4b0b`.

Rollback must use the immediate pre-deploy backup above and the invariant
Compose invocation with `APP_VERSION=8.63.0-61c46c3`, the base project
directory/env file and the retained v8.63.0 Compose file. No database downgrade
was attempted.

After cleanup, free space was 463,524,667,392 bytes on LAN and 6,408,388,608
bytes on VPS. No broad Docker or release cleanup was run.
