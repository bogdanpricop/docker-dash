# V4.4s — Automatic secret-reference admission rollout results

## Scope

Release v8.91.3 closes the repo-local B168 admission gap identified after the
v8.90 qualification pass. It connects the existing hash-only validator to the
manifest, workflow-job and onboarding-template paths that accept documents.

## Release evidence

- feature commit: `378440a`;
- release commit and annotated tag: `ddc55ec` / `v8.91.3`;
- local validation: 316 suites passed, 3,372 tests passed and 4 skipped;
- GitHub CI run `30740094080` passed in 2m17s;
- GitHub Docker build run `30740094103` passed in 50s;
- virtualization registry remained 450 features: 391 `Done`, 59 `Partial`,
  0 `Open`;
- ESLint completed with 0 errors and the 14 known warnings; i18n,
  accessibility, syntax and whitespace gates passed.

## Docker rollout

The exact annotated-tag archive had 1,903 entries, 38,522,880 bytes and SHA-256
`53233aefd64eb1c155f33bfe8cf5b2c5d4f77bba5981067fb30da49417c4356f`.
That digest matched on both targets before extraction.

| Target | Release directory | Image ID | Backup |
|---|---|---|---|
| LAN `SVR-Docker2` | `/home/localadmin-a/docker-dash-releases/v8.91.3-ddc55ec` | `0c9f06dd5fcf` | `predeploy-v8.91.3-ddc55ec.db`, 2,637,807,616 bytes |
| VPS `vps96299.whmpanels.com` | `/opt/docker-dash/releases/v8.91.3-ddc55ec` | `73f4c9099c76` | `predeploy-v8.91.3-ddc55ec.db`, 449,572,864 bytes |

Both backups were created with the SQLite online-backup API while the app was
stopped, retained with mode 0600 and passed `quick_check=ok`. Both containers
then reported `healthy`, version `8.91.3`, restart count 0, live database
`quick_check=ok` and zero critical migration/uncaught/fatal log matches. The
LAN database reported 170 migrations and the VPS retained its expected 169.

External health checks returned 8.91.3 from `192.168.13.20:8101` and
`89.37.212.66:8101`. A runtime smoke inside each deployed container rejected a
secret-shaped inline environment assignment with `INLINE_SECRET_VALUE`,
`documentStored:false` and `networkCallsStarted:0`.

## Remaining boundary

B168 stays `Partial` because the integrated browser runtime exposed no browser
in this session, so browser smoke remains explicitly unrecorded. No provider,
network or external execution is claimed by this rollout.
