# V3.7 — Restore and replication depth rollout results

## Scope

The release candidate covers exactly B139–B148. B139/B140 and B146–B148 reuse
the already-shipped V3.5/V3.6 implementations. B141–B145 add the metadata,
planning, policy, API and UI contracts described by the V3.7 feature spec.

## Current result

- file catalogs are metadata-only and path-safe;
- advanced restore and cross-site copy plans are persistent and hash-bound;
- replication policies are versioned drafts and cannot be enabled;
- existing restore drills and DR rehearsals remain task/evidence backed;
- all new provider mutations remain fail-closed and default-off.

## Release/deployment evidence

Release-candidate validation on 2026-07-31:

- 309 Jest suites passed;
- 3,310 tests passed and 4 were skipped;
- ESLint completed with zero errors and 14 pre-existing warnings;
- self-service i18n and accessibility gates passed;
- virtualization registry validated 450 features: 391 Done, 59 Partial, 0 Open;
- `git diff --check` passed.

Release and rollout:

- commit `f7bf79a`, annotated tag `v8.81.0` and GitHub release published;
- identical Git archive SHA-256
  `ab12e1cc7b8b2a138041e4f49f5ae9469da2eefb2e1e19cdf2f01f2d91f4a844`
  transferred to both targets;
- production target built as `docker-dash:8.81.0` and
  `docker-dash:8.81.0-f7bf79a` on LAN and VPS;
- live SQLite backups retained as
  `/data/backups/predeploy-v8.81.0-f7bf79a.db` (1,863,270,400 bytes on LAN;
  445,526,016 bytes on VPS);
- only Compose service `app` was recreated through the canonical compose file;
- both containers are healthy and both internal/external health endpoints
  report version `8.81.0`;
- migration `166_provider_restore_replication_depth.js` is applied on both
  databases and `PRAGMA integrity_check` returns `ok`;
- transferred remote archives were removed after verification; database
  backups were preserved.

Local Docker/browser smoke remains unavailable in this workstation session.
