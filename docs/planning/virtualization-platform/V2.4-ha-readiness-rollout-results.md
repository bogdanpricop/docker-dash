# V2.4 HA inventory and readiness — rollout results

**Functional and deployed commit:** `e3da04b76f15664a3cbbc1ed24ab061cf4c16c75`

## Delivered

- provider-neutral HA snapshot schema with explicit `ready`, `degraded`, `blocked`, `not_configured`, `unsupported` and `unknown` states;
- read-only collectors for Proxmox Corosync/HA evidence, vCenter DAS configuration and Xen XAPI/XO pool HA fields;
- explicit standalone ESXi and raw-Xen refusal instead of fabricated cluster evidence;
- opaque canonical domain/host/VM identities and encrypted, five-minute snapshot buckets;
- bounded 96-snapshot retention, stale last-good fallback, refresh deduplication and bounded SQLite busy retry;
- weighted readiness signals for HA enablement, coordination, heartbeat, fencing/isolation, shared storage, admission control, failover capacity and protected-workload coverage;
- exhaustive one-to-three-host loss combinations within the safe host bound, with provider-native plan depth taking precedence only for the capacity conclusion it proves;
- descriptive recovery-priority groups that do not claim an application dependency order;
- viewer-scoped current/history APIs, admin-only audited refresh, a leader-gated scheduled collector and a provider-neutral High Availability page;
- release gate `DD_PROVIDER_HA_READINESS`, disabled by default and explicitly enabled on staging;
- additive migration `115_provider_ha_readiness.js`.

## Validation

- focused HA/provider regression: 9 suites and 134 tests passed;
- full Jest: 213 suites passed, 2,654 tests passed, 4 skipped, 2,658 total;
- full ESLint: zero errors (13 pre-existing warnings);
- JavaScript syntax checks and `git diff --check`: clean;
- GitHub CI run `30216736921`: success;
- GitHub Docker Build & Push run `30216736916`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-547ae12`, healthy;
- free root capacity: 495,292,690,432 bytes;
- consistent database backup: `/data/backups/pre-deploy-e3da04b.db`, 2,857,951,232 bytes;
- backup SHA-256: `b9dddecbadad9aeac3ebe3eeb8768f31601b28747ab34c7f314f78b4b40db86c`;
- backup validation: `quick_check=ok`, zero foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-e3da04b/.env`;
- immutable source: `/home/localadmin-a/docker-dash/releases/e3da04b`;
- source archive: 18,807,930 bytes, SHA-256 `ff0ffeea3298b446315d743425bf9e11e009d8a5dd5192f08edd0827cfe78447`.

Deployment evidence:

- image: `docker-dash:8.21.4-e3da04b`;
- image ID: `sha256:1d0757bda6dfe2ee89419a118d1aec68517a65cafe67669cd4f431ce8293818b`;
- OCI revision: exact deployed commit; OCI version: `8.21.4-e3da04b`;
- app: running, healthy, zero restarts;
- `DD_PROVIDER_HA_READINESS=true` is active in the container;
- migration 115 is recorded, the snapshot table has ten expected columns, live `quick_check=ok` and foreign-key violations are zero;
- persistent data/stacks/certificate/egress mounts and auxiliary services were preserved by `compose up --no-build --no-deps app`.

Smoke evidence:

- `/api/health`: HTTP 200;
- High Availability JavaScript page: HTTP 200;
- readiness API without authentication: HTTP 401;
- two configured standalone vSphere endpoints returned `unsupported`, one opaque domain each, no score and no native reference exposure;
- two HA snapshots were persisted encrypted; the smallest encrypted payload was 2,108 bytes;
- zero fatal, uncaught, unhandled, migration-failure or SQLite-corruption log matches.

No HA policy, VM, host, storage, network or provider state mutation was attempted during rollout.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected before authentication. The expected ED25519 fingerprint from `known_hosts` line 8 is:

`SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`

The server currently presents:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The key was not accepted, replaced, deleted or bypassed. Public deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-547ae12` and `/home/localadmin-a/docker-dash/deploy-backups/env-before-e3da04b/.env`. Migration 115 is additive and old code ignores its table, so the database must not be restored automatically. The verified backup is retained for explicit disaster recovery only.
