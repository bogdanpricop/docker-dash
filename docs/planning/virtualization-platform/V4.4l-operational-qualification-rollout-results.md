# V4.4l — Operational qualification rollout results

## Scope

Release v8.85.0 qualifies exactly B015, B045, B090, B096, B104, B118,
B119, B120, B121 and B123. Their implementation was already shipped in
v8.80.0. This release removes the stale release-inclusion blocker and exposes
the remaining browser, adapter, collector, active-runner and canary evidence
without promoting any of the ten rows from `Partial`.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,342 tests passed and 4 were skipped (3,346 total);
- ESLint completed with zero errors and 14 pre-existing warnings;
- self-service i18n and accessibility gates passed;
- the virtualization registry validated 450 features: 391 Done, 59 Partial,
  0 Open;
- backend/frontend syntax and `git diff --check` passed;
- the focused qualification, registry, page and repository-health suites passed;
- the integrated browser runtime exposed no browser, so browser smoke remains
  explicitly `not_recorded`.

GitHub:

- feature commit `43c05ead0e7f80e541abfc565e7c1cd2802ff472`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.85.0`;
- tag Docker run `30665985442` and PR Docker run `30665986710` succeeded;
- CI run `30665986946` initially hit one non-batch Caddy socket test flake;
  the unchanged-commit failed-job rerun passed every CI step.

## Deployment evidence

The Git tag archive contained 1,884 tracked entries, was 20,110,724 bytes and
had SHA-256
`a69f3a1788422530b12ddbc9bd25e529825f0f308eafee5a21617f1f1a712285`.
It contained no `.env`, `.git/` or `node_modules/` entry. Both transferred
archives matched the same digest.

The application was briefly quiesced on each target while the live SQLite file
was copied. v8.84.0 was restarted and healthy before source extraction/build.
The retained rollback copies are:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.85.0-43c05ea.db` | 2,081,132,544 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.85.0-43c05ea.db` | 454,377,472 | 0600 | `ok` |

Each target built the release archive with `docker build --target production`
and tagged `docker-dash:8.85.0`. Only Compose service `app` was force-recreated
with `APP_VERSION=8.85.0`.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:31510ba25c3bad9fa078aff12a9893289bdbcabd691538b430b0f2a85108661a` | `8.85.0`, healthy | 0 |
| VPS | `sha256:1febd1c30dadecd58206ba55c0268e57744185f9b0074cff8fe2a7ded4a7e100` | `8.85.0`, healthy | 0 |

No migration is introduced by v8.85.0. On both databases the latest applied
migration remains exactly `169_provider_privileged_compliance.js`, and live
`PRAGMA integrity_check` returns `ok`.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Execute flags | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 | 10/10 | 1 | 0 | 0 | `0d65ff0cab70bfa0e0cba1380acbd625fb5a1af2aaf09be584e8ab9076520253` |
| VPS | explicit synthetic Proxmox host 1; no active virtualization provider exists | 10/10 | 0 | 0 | 0 | `0f2ce780fdeb1cd68152c74ecfc91984c4605d559775d3867b0bfb3188abd644` |

Both envelopes contain the ordered IDs B015, B045, B090, B096, B104, B118,
B119, B120, B121 and B123. LAN reports one stored runtime observation. VPS
reports zero and does not convert the absent provider/runtime evidence into a
success. `DD_PROVIDER_VM_ACTION_SCHEDULES` and the relevant provider-specific
`DD_PROVIDER_VM_NIC_LINK_*` flag are false on both targets.

On both deployments:

- the protected API returns 401 without authentication, proving route
  registration without exposing qualification data;
- the served UI and deployed source contain the operational qualification
  marker;
- startup/runtime logs contain zero error-level, uncaught or fatal entries;
- `.env` remains mode 0600 and retains its original owner;
- the qualification invocation starts zero provider mutations, network calls
  and external commands.

Temporary local and transferred release archives were removed after
verification. The two rollback database backups were intentionally preserved.
