# V6.5b — Edge sovereignty and resilience rollout

**Release:** v8.66.0

**Feature commit:** `3bbdd18`

**Date:** 2026-07-29

## Outcome

B336–B345 were pushed to `agent/advanced-compose-gitops`, included in PR #13,
tagged `v8.66.0` and deployed as `docker-dash:8.66.0-3bbdd18` to LAN and VPS.
Both app containers are healthy with zero restarts and five mounts. No optional
Compose profile or sidecar was recreated.

## Verification before publish

- Full Jest: 276/276 suites passed; 3,025 tests passed and 4 skipped (3,029
  total).
- Focused edge selection: 22/22 tests passed; the new B336–B345 suite itself
  passes 10/10.
- ESLint: 0 errors and the same 14 historical warnings.
- Syntax checks passed for service, routes, migration, browser API/page and
  What's New; `git diff --check` passed.
- A fresh in-memory migration chain applied migration 140 once, exposed 31
  `edge_*` tables, 65 permissions and both residency sync columns, with SQLite
  `quick_check` equal to `ok`.
- GitHub CI run `30499223557` and Docker build runs `30499223567` and
  `30499222402` succeeded. PR #13 reports a clean merge state.

## Artifact

- Exact Git archive: `docker-dash-8.66.0-3bbdd18.tar.gz`.
- Size: 19,558,881 bytes.
- SHA-256: `de858fb1d91ed95c3bad8d98e2cdb063cb3dcf57d2015afa09a2967fecff9702`.
- The checksum matched after transfer on both hosts. Exact incoming archives
  were removed after verification; extracted release directories remain.

## Backup and deployment evidence

| Target | Runtime | Verified online backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.66.0-3bbdd18`, image ID `sha256:b09cacb0a4ea77eff39875e8b6eba3e27e6fb45eceb7b517a8d5045b03c1c327`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v65b-20260729T232215Z-3bbdd18/docker-dash.db`, 3,759,169,536 bytes, mode 600, SHA-256 `5e6e5b9c780627fe7b154052c03c70e32e92f7d6ecfeb0354fd38bb4ad1dcab2` |
| VPS `89.37.212.66` | image `docker-dash:8.66.0-3bbdd18`, image ID `sha256:600414a8a9cffe17e739a1cfeefd9bab62e6027b4a72fdc2d8f54d51296f2cab`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v65b-20260729T232339Z-3bbdd18/docker-dash.db`, 440,631,296 bytes, mode 600, SHA-256 `5884291fc5d16cef0cbcbb0b3d79b153cb2881ef573db8daa2108a629320334c` |

Each backup used the running application's `better-sqlite3` online backup API.
Before build/recreate, the 8.65.0 image opened the exact backup file read-only
through a direct file mount beneath a writable `/backup` tmpfs, with no network
and a read-only root filesystem. `quick_check` returned `ok`; the previous image
observed 304 tables on both targets.

## Migration and smoke verification

- Migration `140_edge_sovereignty_resilience.js` ran exactly once per target.
- All 31 exact `edge_*` tables exist. Both databases contain 65 governance
  permissions; post-deploy SQLite `quick_check` is `ok`.
- The base `.env` was copied with mode 600 into each extracted release and
  compared byte-for-byte before build. Compose used the base project directory,
  base env file, exact release Compose file, `--no-build --no-deps app` and
  `APP_VERSION=8.66.0-3bbdd18`.
- Internal and external `/api/health` report version `8.66.0`; external `/`
  returns HTTP 200 on LAN and VPS. `/api/edge/overview` returns HTTP 401 without
  authentication.
- Both images were built from the exact release archive with Docker target
  `production` and build argument `APP_VERSION=8.66.0`.

## Rollback readiness

The previous image `docker-dash:8.65.0-a4d2d65` and extracted release remain on
both hosts. Current/previous release paths are:

- LAN current `/home/localadmin-a/docker-dash-releases/v8.66.0-3bbdd18`,
  previous `/home/localadmin-a/docker-dash-releases/v8.65.0-a4d2d65`;
- VPS current `/opt/docker-dash/releases/v8.66.0-3bbdd18`, previous
  `/opt/docker-dash/releases/v8.65.0-a4d2d65`.

Rollback must use the immediate pre-deploy backup and invariant Compose command
with `APP_VERSION=8.65.0-a4d2d65`, the base project directory/env file and the
retained 8.65.0 Compose file. No database downgrade was attempted.

Only the now previous-previous 8.64.0 release/image and exact incoming archive
were removed. After cleanup, free space was 455,567,159,296 bytes on LAN and
10,255,597,568 bytes on VPS. No broad Docker or release cleanup was run.
