# V1.7 guest customization — rollout results

**Functional commit:** `8215774711d1e64480d7f1a6c2c3829556c58430`

## Validation

- full Jest: 193 suites passed; 2,551 tests passed, 4 skipped, 2,555 total;
- targeted guest/provider/client/state-machine tests: green;
- full ESLint: 0 errors, 13 historical warnings;
- JavaScript syntax checks and `git diff --check`: clean;
- GitHub CI run `30208408521`: success;
- GitHub Docker Build & Push run `30208408519`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-140494f`, healthy;
- free root capacity: 489 GB;
- database backup: `/data/backups/pre-deploy-8215774.db`, 2,783,391,744 bytes;
- backup validation: `integrity_check=ok`, 0 foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-8215774/.env`;
- immutable source: `/home/localadmin-a/docker-dash/releases/8215774`;
- source archive SHA-256: `e6e8ae36017928be8418f299c51161929cf1fbc5b6912edb6244f634bc8cdad7`.

Deployment evidence:

- image: `docker-dash:8.21.4-8215774`;
- image ID: `sha256:dd9dc0da0c2bc7ca176596291520031fe4efc2a94f8d74c33f401a1eb9cd37b6`;
- OCI revision: exact functional commit;
- app container: running, healthy, 0 restarts;
- persistent data, stacks, certificates, egress policy and Docker socket mounts preserved.

Smoke evidence:

- `/api/health`: HTTP 200;
- unauthenticated clone preflight: HTTP 401;
- VM Catalog JavaScript: HTTP 200 and guest-customization UI hook present;
- `providerVmProvisioning=false` and `providerVmGuestCustomization=false`;
- feature environment overrides absent, so defaults remain off;
- 0 active provider operations and 0 provider operation locks;
- 0 fatal/uncaught/unhandled/migration-corruption log matches.

No real VM mutation was executed because the release flags remain disabled and no disposable provider-compatible template/cleanup scope was selected. Existing warnings remain: CSRF disabled, insecure cookie flag and seven-day audit retention.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected because the ED25519 host key differs from `known_hosts` line 8. The presented fingerprint remains:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

No key was replaced, accepted or bypassed. Deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-140494f` and `/home/localadmin-a/docker-dash/deploy-backups/env-before-8215774/.env`. V1.7 adds no schema migration. The verified database backup is retained for explicit data recovery only; it must not be restored automatically over a live database.
