# V2.5 affinity and placement advisory — rollout results

## Release identity

- functional commit: `ea1a2036d58096ea3182687a3da5c32e56228d47`;
- branch / review: `agent/advanced-compose-gitops`, draft PR #13;
- CI: run `30218160614` passed;
- Docker build: run `30218160596` passed;
- staging image: `docker-dash:8.21.4-ea1a203`;
- image ID: `sha256:223a5d7d0b0ff0b6ef2af49ae483bf49f995328d49a1330cf4555d2d0eaab4a5`;
- OCI revision: `ea1a2036d58096ea3182687a3da5c32e56228d47`;
- immutable release: `/home/localadmin-a/docker-dash/releases/ea1a203`.

## Validation

- focused: 8 suites and 140 tests passed;
- full repository: 215 suites passed, 2,671 tests passed and 4 explicitly skipped;
- lint: zero errors and 13 pre-existing warnings;
- GitHub CI additionally passed production critical-vulnerability, backend/frontend syntax, lint, tests and i18n gates;
- no new database migration; latest recorded migration remains `115_provider_ha_readiness.js` (`_migrations.id=116`).

The in-app browser skill was initialized for visual QA, but this session exposed no browser instance. DOM/XSS tests and staging HTTP/API smoke were therefore used; visual inspection remains available on the deployed page.

## Backup and transfer evidence

- verified SQLite backup: `/data/backups/pre-deploy-ea1a203.db`;
- backup bytes: `2,881,503,232`;
- backup SHA-256: `17dc25deea1fdd42ca9a2ad459f7ec79406001d76718bce0d1982fb42c6213c7`;
- backup `quick_check`: `ok`; foreign-key violations: `0`;
- configuration backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-ea1a203/.env`;
- exact Git archive: `18,832,098` bytes, SHA-256 `8c5f0db23f76e3e93e5ce8d9151128a604143b8a33d579830b21b0032f60a215`;
- the uploaded archive was hash-verified and removed after extraction; the local temporary copy remained under the OS temporary directory because the execution policy rejected its explicit deletion.

## Staging smoke

- target: `localadmin-a@192.168.13.20`, strict known-host verification;
- container: healthy, restart count `0`;
- `/api/health`: `status=ok`, version `8.21.4`;
- `DD_PROVIDER_PLACEMENT_ADVISORY=true`; existing `DD_PROVIDER_HA_READINESS=true` retained;
- unauthenticated placement request returned `401`;
- Placement Advisor JavaScript asset was served;
- authenticated endpoint 5 smoke: affinity capability `unsupported`, zero rules, five VMs inventoried, one recommendation candidate, zero rebalance moves, plan `executable=false`, 64-character semantic hash;
- temporary two-minute smoke session count after cleanup: `0`;
- audit action `provider_placement_rebalance_plan` recorded for endpoint `5`;
- live DB `quick_check=ok`, foreign-key violations `0`;
- no fatal, unhandled or migration-error log signature after deployment.

Standalone ESXi is expected to return placement policy and alternate-target capability as unsupported. This is a successful fail-closed result, not a false empty implementation claim.

## VPS gate

Strict SSH verification stopped before authentication on `root@89.37.212.66:2224`:

- pinned ED25519: `SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`;
- presented ED25519: `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`.

No known-host entry was altered and no bypass was used. VPS deployment remains blocked until the presented key is verified out-of-band.

## Rollback

- previous image: `docker-dash:8.21.4-e3da04b`;
- restore configuration from `deploy-backups/env-before-ea1a203/.env`;
- run Compose with project directory `/home/localadmin-a/docker-dash` and the V2.4 release file;
- no database rollback is normally needed because V2.5 adds no migration; the verified backup remains available for incident recovery only.
