# V2.2 native VM migration — rollout results

**Functional commit:** `da93c0990d91b68c926de9c468f8b575c425b2c0`

## Delivered

- durable `vm.migrate` operations with encrypted canonical plans, idempotency keys, VM/host/storage locks, progress, cancel, reconcile, and post-placement verification;
- release flag `DD_PROVIDER_VM_MIGRATION`, disabled by default and explicitly enabled on staging;
- live mode restricted to running VMs and cold mode restricted to already-stopped VMs;
- exact-name confirmation, five-minute semantic plan, live policy/permission/conflict/identity gates, and immediate provider revalidation;
- Proxmox QEMU/LXC native migration UPIDs, including explicit local-disk storage mapping and LXC live-migration refusal;
- vSphere `RelocateVM_Task`, target host/datastore revalidation, task progress, `info.cancelable`, and `CancelTask`;
- XenServer/XCP-ng `Async.VM.pool_migrate`, XAPI task polling/cancellation, and raw-Xen refusal;
- Xen Orchestra execution only after runtime OpenAPI discovery of a recognized task-backed migration action and target field;
- response-loss recovery by observed placement without replaying a non-idempotent mutation;
- operator-only preflight/submission routes, audit event, release-gated Migration-tab actions, typed review, and Activity Center hand-off;
- legacy URL-to-Proxmox import remains separate and unchanged.

## Validation

- focused migration and provider-operation regression: 13 suites and 116 tests passed;
- full Jest: 207 suites passed; 2,615 tests passed, 4 skipped, 2,619 total;
- full ESLint: zero errors and 13 historical warnings in unrelated modules;
- JavaScript UI lint and `git diff --check`: clean;
- GitHub CI run `30213537511`: success;
- GitHub Docker Build & Push run `30213537545`: success.

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-6e132e9`, healthy, zero restarts;
- free root capacity: 508,505,837,568 bytes;
- consistent database backup: `/data/backups/pre-deploy-da93c09.db`, 2,834,178,048 bytes;
- backup SHA-256: `786685d5ddcbd39290a6224e95789ec22f0237b72940c8c9a04392ed772bdc09`;
- backup validation: `quick_check=ok`, zero foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-da93c09/.env`;
- immutable source: `/home/localadmin-a/docker-dash/releases/da93c09`;
- source archive SHA-256: `b5cbbfe3093d2b23bcb11c89999eaa1151163a6bb64a1b8ed594cc704ccb6be0`.

Deployment evidence:

- image: `docker-dash:8.21.4-da93c09`;
- image ID: `sha256:21e2a21ed753073e3bb8c76e3b1c099c0a271549545b9e1553e7217bc91155d8`;
- OCI revision: exact functional commit;
- app: running, healthy, zero restarts;
- persistent database, stacks, certificates, egress policy, and read-only Docker socket mounts preserved;
- `DD_PROVIDER_VM_MIGRATION=true` is active in the container.

Smoke evidence:

- `/api/health`: HTTP 200;
- common VM JavaScript: HTTP 200 and contains release-gated migration review/submission controls;
- unauthenticated migration execution preflight: HTTP 401;
- unauthenticated migration submission: HTTP 401;
- `vm.migrate` is conditionally declared for Proxmox, vSphere, and managed XAPI;
- durable `vm.migrate` handler is registered in the deployed operation engine;
- live database: `quick_check=ok`, zero foreign-key violations;
- zero fatal, uncaught, unhandled, migration-failure, or corruption log matches.

No provider credential or native identifier was exposed, and no real migration or other provider mutation was attempted during rollout.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected before authentication because the ED25519 host key still differs from `known_hosts` line 8. The presented fingerprint is:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The key was not accepted, replaced, deleted, or bypassed. Public deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-6e132e9` with `/home/localadmin-a/docker-dash/deploy-backups/env-before-da93c09/.env`. V2.2 adds no schema migration. The verified database backup is retained only for explicit disaster recovery and must not be restored automatically over a live database.
