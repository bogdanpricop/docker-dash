# V6.3a — FinOps cost foundation rollout

**Release:** v8.60.0  
**Feature commit:** `c8ed49b`  
**Date:** 2026-07-29

## Outcome

V6.3a was pushed to `agent/advanced-compose-gitops`, tagged `v8.60.0` and
deployed as `docker-dash:8.60.0-c8ed49b` to the LAN and VPS targets. Both app
containers are healthy with zero restarts and five mounts. No optional profile
service was recreated.

## Verification before publish

- Full Jest run: 269/269 suites passed; 2,948 tests passed and 4 skipped
  (2,952 total).
- Dedicated V6.3a run: 14/14 tests passed.
- ESLint: 0 errors; 14 pre-existing warnings.
- Browser/API JavaScript syntax and `git diff --check`: passed.
- Release source archive: 19,412,011 bytes; SHA-256
  `84f531d4cd92b54b282529370bd9099b57a9e90675848812fd848da97bf1b8d5`.

## Backups and deployments

| Target | Deployment | Verified online SQLite backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.60.0-c8ed49b`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v63a-20260729T203920Z-c8ed49b/docker-dash.db`, 3,746,947,072 bytes, SHA-256 `16ead79f7956e0bc57babb42165485bb927129618fa1ab975bcc7d465623b225` |
| VPS `89.37.212.66` | `docker-dash:8.60.0-c8ed49b`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v63a-20260729T203921Z-c8ed49b/docker-dash.db`, 437,395,456 bytes, SHA-256 `869488b9d0b2fe947ee90ab6ff03d34f3ffc702cbf7f2545d75608cc259e6a07` |

Each backup used the running application's online `better-sqlite3` backup API,
was copied outside the volume with mode 0600 and was mounted directly read-only
into the previous production image for `PRAGMA quick_check`. A private env copy
and container inspection are retained alongside each database.

## Deployment and rollback method

Both hosts checksummed and extracted the same Git archive, then built the
production target with `APP_VERSION=8.60.0`. Only the app was recreated:

```sh
APP_VERSION=8.60.0-c8ed49b docker compose \
  --project-directory "$BASE" --env-file "$BASE/.env" \
  -f "$RELEASE/docker-compose.yml" up -d --no-build --no-deps app
```

The verification script retained an app-only rollback to V0.3f. All checks
passed and no rollback was required.

## Post-deploy evidence

- Internal and external `/api/health` report `8.60.0`; `/` returns HTTP 200.
- Unauthenticated `/api/governance/lifecycle/finops` returns HTTP 401.
- Migration `134_finops_cost_foundation.js` is applied exactly once.
- All eight exact FinOps tables exist and the permission catalog contains 44
  entries.
- Live `PRAGMA quick_check` is `ok` on both hosts.
- The exact source archives were removed after success; the immediately prior
  image and release remain for rollback.
- LAN retains about 472 GB free and VPS about 7.6 GB free.

Startup logs contain no V6.3a error. Existing environment warnings remain: LAN
has disabled CSRF, insecure cookies, short audit retention and the paused
`Romarg` SSH credential; VPS has insecure cookies and short audit retention.
