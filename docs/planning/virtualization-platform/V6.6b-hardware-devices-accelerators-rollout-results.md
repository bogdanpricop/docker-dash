# V6.6b — Hardware devices and accelerator rollout

**Release:** v8.71.0

**Feature commit:** `254f93d`

**Date:** 2026-07-30

## Outcome

B386–B395 were implemented, tagged `v8.71.0`, pushed to draft PR #13 and
deployed as `docker-dash:8.71.0-254f93d` to LAN and VPS. Migration 145 is
present exactly once on both databases, all four device/accelerator tables are
present and the governance catalog contains 83 permissions.

## Validation before rollout

- Focused validation: 3 suites and 24 tests passed.
- Full validation: 282 suites passed; 3,079 tests passed and 4 were skipped.
- ESLint: 0 errors and the existing 14 warnings.
- Self-service localization: 11 languages, 19 keys and 209 values passed.
- Accessibility contract: 8 UI and 5 CSS checks passed.
- Fresh database: migration 145 applied once, four tables created and
  `hardware_devices.manage` increased the catalog to 83 permissions.
- GitHub CI run `30507409915` and Docker Build & Push run `30507409933`
  completed successfully for commit `254f93ddb7f3fbbc6cd802f769529e43af3e288e`.
- Interactive browser discovery returned no available browser in this session;
  frontend syntax, UI source contracts and the repository integration suite
  were used as the UI gate.

## Artifact

- Exact Git archive: `docker-dash-8.71.0-254f93d.tar.gz`.
- Size: 19,670,549 bytes.
- SHA-256: `7845630091c1c68d27d41e0e351374cbb8e5141135e1b9fc6ffc947d5f6866d6`.
- Both targets recomputed and matched the checksum before extraction/build.
- Both builds used the Dockerfile `production` target and the exact release
  directory. The existing `.env` checksum was unchanged across build/deploy.

## Backup and deploy evidence

| Target | Runtime | Backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.71.0-254f93d`, image ID `sha256:e88fce6d81ae310c45bd733e061a0ce5218226b89a08fceb4ac5fd18fafdd687`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v66b-20260730T020905Z-254f93d/docker-dash.db`, 3,772,141,568 bytes, mode 600, SHA-256 `54953dbd384a25fd6b4fea3639cfc3061d92bdbdf394b275b3c4a321328535ae` |
| VPS `89.37.212.66` | image `docker-dash:8.71.0-254f93d`, image ID `sha256:c9afd65b5a95d3d798d3463696ae33a6726d0f4277e9418679d4fbc8f51a86f8`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v66b-20260730T021118Z-254f93d/docker-dash.db`, 443,531,264 bytes, mode 600, SHA-256 `87e91cb49b5d31f8a367e54b4d32d91c00777e3b24c3b80110dbd6a70d73fccf` |

Each backup was produced with the running application's `better-sqlite3`
online backup API, copied out of the container and validated as read-only in a
networkless, read-only container using the previous release image.
`PRAGMA integrity_check` returned `ok` on both targets.

The first LAN attempt stopped during a pre-backup textual version check because
of command quoting. The running 8.70.0 container was not touched; deployment
resumed from the checksum-verified extracted release and completed every backup,
build, migration and health gate.

## Post-deploy checks

- LAN and VPS `/api/health` report `status=ok`, version `8.71.0` and standalone
  mode from both localhost and external addresses.
- The unauthenticated hardware-device endpoint returns HTTP 401 on both targets.
- Migration/table/permission query returns
  `{migration145:1,tables:4,permissions:83}` on both targets.
- Remaining free space: 458,321,145,856 bytes on LAN and 5,857,566,720 bytes on
  VPS.
- Immediate rollback is retained as release/image `8.70.0-3b7fb63` on both
  targets. Exact release/image `8.69.0-671dc66` was removed after validation.
