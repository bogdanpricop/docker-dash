# V1.6 VM provisioning — rollout results

**Functional commit:** `140494f731611c6cf6d0466b04cd36209034d48d`

**Date:** 2026-07-26

## Local and CI evidence

- 191 Jest suites passed;
- 2,535 tests passed, 4 skipped / 2,539 total;
- ESLint: 0 errors, 13 historical warnings;
- GitHub CI run `30206944886`: success;
- Docker Build & Push run `30206944889`: success;
- draft PR: #13, branch `agent/advanced-compose-gitops`.

## Staging preflight and backups

Target: `localadmin-a@192.168.13.20`, strict host-key checking.

- source image: `docker-dash:8.21.4-0d88227`, healthy;
- active provider operations: 0; active operation locks: 0;
- DB backup: `/data/backups/pre-deploy-140494f.db`, 2,773,434,368 bytes;
- backup verification: `integrity_check=ok`, foreign-key violations 0;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-140494f/`;
- immutable release: `/home/localadmin-a/docker-dash/releases/140494f`.

## Deployment and smoke

- image: `docker-dash:8.21.4-140494f`;
- OCI revision: `140494f731611c6cf6d0466b04cd36209034d48d`;
- final state: healthy, restart count 0;
- `/api/health`: HTTP 200;
- unauthenticated clone preflight: HTTP 401;
- VM Catalog asset: HTTP 200, `Create VM` hook present;
- provider route and durable provisioning handler present in the runtime image;
- `DD_PROVIDER_VM_PROVISIONING` absent from the runtime environment, therefore the default-OFF rollout control remains effective;
- active provider operations and locks remained 0;
- fatal/uncaught/unhandled/migration-error scan after startup: 0 lines.

No live VM mutation was executed. Enabling provisioning requires an explicit configuration change followed by provider-specific disposable-resource validation.

## Public VPS

Deployment remains blocked before authentication because strict SSH verification reports a changed ED25519 key for `[89.37.212.66]:2224`:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The stored offending entry is `known_hosts` line 8. No key was replaced and strict checking was not bypassed.

## Rollback

The application rollback target is `docker-dash:8.21.4-0d88227` plus `/home/localadmin-a/docker-dash/deploy-backups/env-before-140494f/.env`. The batch adds no schema migration. A data rollback, only if explicitly required, uses the verified backup after first stopping the app and preserving the current database separately.
