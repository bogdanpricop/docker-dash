# V4.4p — Security lifecycle operational qualification rollout results

## Scope

Release v8.89.0 qualifies exactly B157–B166. B157–B158 were implemented in
v8.82.0 and B159–B166 in v8.83.0. The release reads scoped security-lifecycle
evidence without starting a provider mutation, network call or external
command, and keeps every external execution/collector gap `Partial`.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,350 tests passed and 4 were skipped (3,354 total);
- 5 focused suites and 35 focused tests passed;
- ESLint completed with zero errors and 14 pre-existing warnings;
- registry validation remained 450 features: 391 Done, 59 Partial, 0 Open;
- i18n, accessibility, syntax and whitespace gates passed;
- the integrated browser runtime exposed no browser, so smoke remains
  explicitly `not_recorded`.

GitHub:

- feature commit `b8330a46ed2566b6559d849cdfa6a48ab7c2fd10`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.89.0`;
- CI run `30673461868`, tag Docker run `30673461709` and PR Docker run
  `30673461919` all succeeded;
- draft PR `https://github.com/bogdanpricop/docker-dash/pull/13` tracks the
  release branch.

## Deployment evidence

The exact tag archive contained 1,892 entries, was 20,133,643 bytes and had
SHA-256
`58dde3211205ec4d29a2c4f5d458f3895966c2bd8a53c0f5f22babe0ba38d186`.
It excluded `.env`, `.git/` and `node_modules/`; both transfers matched the
digest and extraction preserved each `.env` digest, owner and mode.

The application was quiesced only while copying SQLite. v8.88.0 was restarted
healthy before extraction/build. Retained rollback copies:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.89.0-b8330a4.db` | 2,105,741,312 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.89.0-b8330a4.db` | 454,377,472 | 0600 | `ok` |

Each target built `docker-dash:8.89.0` from the verified archive and only
Compose service `app` was force-recreated.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:4122cd4d5e38f0677837bcfcf87fea4fe7de5c3f001f754a0a19d9c72ed3a526` | `8.89.0`, healthy | 0 |
| VPS | `sha256:7046e9e1e21576869483f3babfbc12f3bda81932e0743d653a74a3cb0c2db0ab` | `8.89.0`, healthy | 0 |

No migration is introduced. The latest applied migration remains
`169_provider_privileged_compliance.js` (170 recorded rows on LAN and 169 on
VPS); live and backup `PRAGMA integrity_check` return `ok`.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Enabled flags | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 (`ESXi-6`) | 10/10 | 0 | 2 | 0 | `7be7e2ad8377903f48d314ad129491956cd2f9c805adb4c594739e3135c39816` |
| VPS | synthetic Proxmox host 1; no active provider exists | 10/10 | 0 | 2 | 0 | `7ef31d444ffb1e9cea0c176c47a20a905303dea7ee497f8158c196ca277488cb` |

Both envelopes contain exact ordered IDs B157–B166, mixed implementation
releases v8.82.0/v8.83.0 and stable hashes. Both targets retain
`DD_PROVIDER_SECURITY_ASSURANCE=true` and
`DD_PROVIDER_SECURITY_LIFECYCLE=true`. Flags are not interpreted as evidence:
B157 confidential-VM provisioning, B162 tracked certificate rotation, B163
CVE-bearing findings, B165 active exceptions and B166 remediation dry-runs all
remain `not_observed` with zero scoped records.

The foundation, network-backup, recovery-depth and DR/security batches retain
their exact IDs, 10/10 schema, stable hashes and zero safety counters. On both
targets the protected endpoint returns 401 without a session, runtime logs
contain zero error/fatal/uncaught entries, and internal plus workstation health
report v8.89.0 with zero restarts.

Temporary release archives were removed locally and remotely. Both rollback
database backups were intentionally preserved.
