# V4.2a — Storage posture rollout results

**Release:** v8.23.0 (`8b98069`) — 2026-07-28

## Validation

- focused storage posture, page and provider route tests: 49 passed;
- complete suite: 239 suites passed, 2,775 tests passed, 4 skipped (2,779 total);
- full ESLint: 0 errors; 13 pre-existing warnings outside this slice;
- syntax checks passed for the new page, posture service and provider routes;
- local Docker Desktop/CLI was unavailable, so production images were built on each deployment target with `docker build --target production`.

## Deployment

| Target | Release image | Backup | Result |
|---|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.23.0-8b98069` | `backups/v42-20260728T093000Z-8b98069/docker-dash.db` SHA-256 `8eaa89ae6705f0e7e7711fb721e0b418aa20524fc97886c66200f39c03683518` | healthy |
| VPS `89.37.212.66` | `docker-dash:8.23.0-8b98069` | `backups/v42-20260728T094000Z-8b98069/docker-dash.db` SHA-256 `b6b4b86d4d159e9ac8cb30629a94c7a252a47ea0c33b32ed3a59a5fcf0daed1d` | healthy |

Both targets returned `/api/health` version `8.23.0`; the new page asset returned HTTP 200 and the new API route returned HTTP 401 without a session, proving the route remains fail-closed. `DD_PROVIDER_VM_DISK_LIFECYCLE` and `DD_PROVIDER_VM_DISK_DELETE` were both verified `false` on both targets. No storage mutation was submitted.

## Deferred verification

An authenticated Storage Posture API read requires an existing permitted application session. No credentials were used or created during rollout. The deployed assets, health state and unauthenticated route guard were verified; a host-view user can now perform the live provider-inventory smoke in the UI.
