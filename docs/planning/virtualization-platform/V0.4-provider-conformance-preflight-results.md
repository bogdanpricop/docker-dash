# V0.4 Preflight și validation results

**Data:** 26 iulie 2026

**Batch:** V0.4 Provider conformance kit

**Status implementare:** implementat, publicat și validat live pe staging

## Implementare

- migrarea aditivă 108 pentru runs și checks persistente;
- manifest schema `1.0`, ownership, compatibility evidence, release rings și deprecations;
- fixture corpus pentru Proxmox, vSphere, Xen XO/XAPI/raw;
- fault injection executat pentru timeout, auth expiry, partial response, redirect și task loss;
- circuit breaker closed/open/half-open și adaptive concurrency budget per endpoint;
- contract runner reutilizabil pentru adaptere built-in și pluginuri viitoare;
- live read-only certification cu bounded inventory și identity stability;
- startup recovery închide run-urile întrerupte ca `failed`, cu check și hash explicit;
- grade/scor, evidence SHA-256 determinist și leak scan;
- host-scoped RBAC, admin run/export și audit;
- scorecard shipped/partial/planned cu SLO baseline;
- export JSON portabil cu integrity hash și retention configurabil;
- Prometheus counter low-cardinality per provider/grade.

## Verificări locale

- 6 suite focused/regression, 54 teste trecute;
- full suite: 177 suite trecute;
- 2.438 teste trecute, 4 skipped, 2.442 total;
- ESLint: 0 erori, 13 warnings preexistente;
- fresh schema din suita completă a aplicat migrarea 108;
- upgrade direct 107→108 și reaplicare idempotentă validate;
- SQLite `foreign_key_check`: fără erori;
- scan static: runner-ul apelează numai `probe` și `listResources`, fără metode mutation;
- două run-uri identice produc același evidence hash;
- concurența dublă pe același host este respinsă cu 409;
- `git diff --check`: curat.

## Deploy gates

- commit funcțional `b23e355`, deadline fix `b43b9a7` și recovery fix `b795b9a`; CI și Docker Build & Push au trecut pentru commit-ul final;
- staging rulează `docker-dash:8.21.4-b795b9a`, healthy, cu 0 restarturi;
- backup DB consistent pre-final: `/data/backups/pre-deploy-b795b9a.db` (2.698.887.168 bytes);
- backup configurație: `/home/localadmin-a/docker-dash/deploy-backups/env-before-b795b9a`;
- migrarea 108 a fost aplicată la `2026-07-26 11:22:32`;
- startup recovery a închis run-ul de smoke întrerupt: 0 `running`, 1 check `run.interrupted`;
- ruta scorecard neautentificată răspunde 401;
- live ESXi run `pcr_610b67faefe407ab6db9301026`: `passed`, grade `certified`, 187/187, fără warning/failure;
- endpoint evidence: ESXi 8.0.2, capability latency 145 ms, 5 VM-uri, 1 host, 1 storage și 1 network validate;
- identity stability a trecut; resilience final: circuit `closed`, active/queued 0, concurrency 2 și 0 failures;
- evidence hash: `17c3ed14e485cbd36119b38258a652ff68027a3fb221dddc56fa989e9e52feae`;
- VPS: blocat până la confirmarea out-of-band a noii amprente ED25519;
- rollback: V0.3 ignoră tabelele 108; datele de conformance rămân disponibile pentru revenire.
