# V3.2 — Backup policy and retention rollout results

**Functional commit:** `ec316750ec681082d595193ed81c9ddf6fa31436`

**Date:** 26 July 2026

## Delivered

- additive migration `118_provider_backup_policies.js` with local policy definitions and immutable plan-evidence rows;
- strict common policy contract for canonical repository/scope, timezone schedule, consistency, portable GFS, encryption, immutability, concurrency, bandwidth, verification, and restore-drill intent;
- live repository and workload preflight with tri-state fail-closed security requirements;
- deterministic policy/plan hashes, local-time due-slot deduplication, and sanitized blocked plans during provider outages;
- soft-delete policy lifecycle, host-scoped viewer reads, administrator authoring, writeability guard, and audit events;
- leader-gated plan-only scheduler with no provider mutation edge;
- Backup Policies UI and API client, explicitly separated from VM snapshots and backup execution;
- feature, deep, and assumption specifications based on current official PBS and XO documentation.

## Verification

### Local

- targeted: 4 suites, 45 tests passed;
- complete: 221 suites passed, 2,706 tests passed, 4 pre-existing skips;
- lint: 0 errors, 13 pre-existing warnings;
- syntax and `git diff --check`: passed;
- migration 118 passed in the full from-zero migration chain;
- secret scan: no credential or key material; the only private-key marker is an intentional negative validation fixture.

### GitHub

- branch: `agent/advanced-compose-gitops`;
- draft PR: `#13`;
- CI run `30221757672`: success for the exact functional SHA;
- Docker Build & Push run `30221757674`: success for the exact functional SHA.

## Staging deployment — `192.168.13.20`

- strict SSH host-key verification passed;
- source archive: 18,911,628 bytes;
- archive SHA-256: `980cad36dd677142a30812c6b88b3cc557fd941e0850cf867fd64da27b4f80e6`, identical before and after transfer;
- immutable release: `/home/localadmin-a/docker-dash/releases/ec31675`, mode `555`;
- backup: `/home/localadmin-a/docker-dash/backups/v32-20260726T215017Z-ec31675`;
- SQLite backup: 2,914,742,272 bytes, mode `600`;
- SQLite SHA-256: `6a02dbc657f4ff9cda88cb25de47e15a9680fac3ff24302b9fe8cb66a3aa27bf`;
- SQLite `quick_check`: `ok`; foreign-key violations: `0`;
- `.env` and compose backup mode: `600`;
- image: `docker-dash:8.21.4-ec31675`;
- image ID: `sha256:2db23b21d2b4b58327d8fd880df25a489befa34d6e3f761daa5a78dea6124e92`;
- OCI revision: exact functional SHA;
- migration `118_provider_backup_policies.js` applied; both tables present;
- `DD_PROVIDER_BACKUP_POLICIES=true`; V3.1 inventory flag remains true;
- final container: healthy, 0 restarts.

### Staging smoke

- `/api/health`: 200;
- application page: 200;
- `/js/pages/backup-policies.js`: 200;
- unauthenticated policy inventory: 401;
- authenticated policy inventory: 200, `executionAuthorized:false`, 0 current policies;
- ESXi preflight: 400 `PROVIDER_RECOVERY_POINT_UNAVAILABLE` with sanitized message, proving unsupported backup evidence fails closed;
- temporary smoke sessions after each harness run: 0;
- migration logs show migration 118 started and completed without application errors.

The staging fleet has no Proxmox/PBS or Xen Orchestra repository endpoint, so a live successful provider-specific policy could not be created. The compatible path is covered by adapter/catalog fixtures and contract tests; the real ESXi endpoint exercised the negative capability path.

The first read-only backup verification container could not open SQLite auxiliary files on a read-only bind. Verification was repeated against the isolated backup with a writable bind, matching SQLite WAL requirements; `quick_check` and FK checks passed. The active database was never mounted by that verifier.

## VPS deployment — `89.37.212.66:2224`

Deployment remains blocked before authentication. Strict checking expects the pinned key at `known_hosts` line 8, while the endpoint presents ED25519 fingerprint:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

No key was accepted, replaced, removed, or bypassed. Deployment requires independent out-of-band verification of the new fingerprint.

## Rollback

- prior image retained: `docker-dash:8.21.4-3cf80eb`;
- prior immutable release retained: `/home/localadmin-a/docker-dash/releases/3cf80eb`;
- pre-deploy DB/config backup is verified and retained;
- migration 118 is additive, and the previous application ignores the new tables;
- rollback is compose `app` only, using the backed-up `.env` and prior release/image.
