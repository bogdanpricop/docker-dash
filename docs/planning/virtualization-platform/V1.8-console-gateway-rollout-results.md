# V1.8 VM console gateway — rollout results

**Feature commit:** `aff75d31e4e8edba2a473a88ea5a198581ed0806`

**Final routing fix:** `f7fde382c61b529bfa055db0583cf0ae297e04ad`

## Validation

- full Jest: 197 suites passed; 2,570 tests passed, 4 skipped, 2,574 total;
- targeted broker, RFB, provider-client, UI and WebSocket routing tests: green;
- full ESLint: 0 errors, 13 historical warnings;
- JavaScript syntax checks and `git diff --check`: clean;
- production audit: 6 advisories, down from 7 after upgrading `ws` to 8.21.1 and removing its console-relevant DoS advisory; no noVNC advisory;
- noVNC build reproducible locally; committed/runtime bundle SHA-256 `82cc762f37e37afa95dfd35173b0476e0862fb71e7e340c12824c268f57dd885`;
- final GitHub CI run `30210417836`: success;
- final GitHub Docker Build & Push run `30210417819`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-8215774`, healthy;
- free root capacity: 486 GB;
- database backup: `/data/backups/pre-deploy-aff75d3.db`, 2,795,036,672 bytes;
- backup SHA-256: `eb066cd13388fcd610adc1198b619f5c205e470072ac6d1fa2c2050f19aa9439`;
- backup validation: `integrity_check=ok`, 0 foreign-key violations;
- original config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-aff75d3/.env`;
- intermediate config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-f7fde38/.env`;
- immutable final source: `/home/localadmin-a/docker-dash/releases/f7fde38`;
- final source archive SHA-256: `96a4471e4d83da82b6c37c9e2c32add8026fa615baba31830917905f3ba06b88`.

Deployment evidence:

- image: `docker-dash:8.21.4-f7fde38`;
- image ID: `sha256:a8fe1d176f60540d52b468d4ac1817ba21706901b7975bd8e7c078243e334329`;
- OCI revision: exact final routing-fix commit;
- app container: running, healthy, 0 restarts;
- persistent database, stacks, certificates, egress policy and read-only Docker socket mounts preserved.

Smoke evidence:

- `/api/health`: HTTP 200;
- console HTML, JavaScript, CSS and noVNC bundle: HTTP 200;
- unauthenticated console preflight and token issue: HTTP 401;
- unauthenticated dedicated WebSocket upgrade: HTTP 401;
- migration tables `provider_console_sessions` and `provider_console_access_locks` present;
- database `quick_check=ok`; 0 active console sessions and 0 console locks;
- `providerVmConsole=false`; provisioning and guest customization also remain disabled;
- 0 fatal/uncaught/unhandled/migration-corruption log matches.

The first staging smoke caught an existing `/ws` listener rejecting the new dedicated path with HTTP 400. The final fix moved the shared endpoint to explicit `noServer` path routing and added a regression test; after redeploy the same handshake reaches the console authorization gate and correctly returns HTTP 401 without a session.

No live VM console was opened because the release flag remains disabled and no disposable running VM/provider endpoint was selected. Provider credentials were never requested or exposed during infrastructure smoke.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` remains blocked before authentication. The server presents ED25519 fingerprint:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

It conflicts with `known_hosts` line 8. No key was replaced, accepted or bypassed; deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-8215774` plus `/home/localadmin-a/docker-dash/deploy-backups/env-before-aff75d3/.env`. Migration 113 is additive, so the prior application safely ignores the two new empty console tables. The verified database backup is retained for explicit disaster recovery only and must never be restored automatically over a live database.
