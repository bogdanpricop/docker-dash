# V6.5a — Edge and disconnected foundation rollout

**Release:** v8.65.0

**Feature commit:** `a4d2d65`

**Date:** 2026-07-29

## Outcome

B326–B335 were pushed to `agent/advanced-compose-gitops`, included in PR #13,
tagged `v8.65.0` and deployed as `docker-dash:8.65.0-a4d2d65` to LAN and VPS.
Both app containers are healthy with zero restarts and five mounts. No optional
Compose profile or sidecar was recreated.

## Verification before publish

- Full Jest: 275/275 suites passed; 3,015 tests passed and 4 skipped (3,019
  total).
- Focused edge/governance selection: 15/15 tests passed; the new B326–B335
  suite itself passes 12/12.
- ESLint: 0 errors and the same 14 historical warnings.
- Syntax checks passed for service, routes, migration, browser API/page/app and
  What's New; `git diff --check` passed.
- GitHub CI run `30497615831` and Docker build runs `30497615792` and
  `30497615559` succeeded. PR #13 reports a clean merge state.

## Artifact

- Exact Git archive: `docker-dash-8.65.0-a4d2d65.tar.gz`.
- Size: 19,534,636 bytes.
- SHA-256: `37c60a82f9175d0e76964780bc46827c3cf30972310d3a4fde12e458a5ddde56`.
- The checksum matched after transfer on both hosts. Exact incoming archives
  were removed after verification; extracted release directories remain.

## Backup and deployment evidence

| Target | Runtime | Verified online backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.65.0-a4d2d65`, image ID `sha256:76f2403f0037e8170d1eaec3f79eff7f6be323f885cbb777c4f6eda5c91436d3`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v65a-20260729T225326Z-a4d2d65/docker-dash.db`, 3,759,169,536 bytes, mode 600, SHA-256 `05ea146f73ad5ed3f927767025705055bc05ce725a2a1bc1262e43c2ed50023c` |
| VPS `89.37.212.66` | image `docker-dash:8.65.0-a4d2d65`, image ID `sha256:7084daa6658638c0381140ce0f12eae266949212481ce7ae5540131f75aa20e0`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v65a-20260729T225327Z-a4d2d65/docker-dash.db`, 440,360,960 bytes, mode 600, SHA-256 `9b63d33e7a8d20883ed73c6264d63c31f077ae3fb7d214cbdcd97239ff9753ed` |

Each backup used the running application's `better-sqlite3` online backup API.
Before build/recreate, the 8.64.0 image opened the exact backup file read-only
through a direct file mount beneath a writable `/backup` tmpfs, with no network
and a read-only root filesystem. `quick_check` returned `ok`; the previous image
observed 289 tables.

## Migration and smoke verification

- Migration `139_edge_disconnected_foundation.js` ran exactly once per target.
- All fifteen exact `edge_*` tables exist. Both databases contain 61 governance
  permissions and three update rings; post-deploy SQLite `quick_check` is `ok`.
- Internal and external `/api/health` report version `8.65.0`; external `/`
  returns HTTP 200 on LAN and VPS. `/api/edge/overview` returns HTTP 401 without
  authentication.
- The production build uses target `production`; `npm ci --omit=dev` still
  reports six known advisories (one low, two moderate and three high).
- Existing LAN warnings remain: CSRF disabled and paused Romarg SSH credentials.
  No environment security setting was modified by this batch.

## Rollback readiness

The previous image `docker-dash:8.64.0-d7f4b0b` and extracted release remain on
both hosts. Current/previous release paths are:

- LAN current `/home/localadmin-a/docker-dash-releases/v8.65.0-a4d2d65`,
  previous `/home/localadmin-a/docker-dash-releases/8.64.0-d7f4b0b`;
- VPS current `/opt/docker-dash/releases/v8.65.0-a4d2d65`, previous
  `/opt/docker-dash/releases/8.64.0-d7f4b0b`.

Rollback must use the immediate pre-deploy backup and invariant Compose command
with `APP_VERSION=8.64.0-d7f4b0b`, the base project directory/env file and the
retained 8.64.0 Compose file. No database downgrade was attempted.

After cleanup, free space was 459,493,183,488 bytes on LAN and 11,700,244,480
bytes on VPS. No broad Docker or release cleanup was run.
