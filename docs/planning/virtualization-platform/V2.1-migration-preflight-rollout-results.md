# V2.1 migration preflight — rollout results

**Functional commit:** `6e132e9d78a0b180751350b6c27b17177a03197c`

## Delivered

- read-only same-endpoint migration target discovery with live, cold, and storage mode evidence;
- explicit same-endpoint/cross-provider boundary and no migration execution route;
- common host availability, maintenance, free-memory reserve, and current-host blockers;
- bounded duration/downtime estimates labeled with methodology and low confidence;
- Proxmox QEMU/LXC migration preconditions plus target storage and bridge inventory;
- vCenter CPU/software VMotion compatibility while keeping datastore/network reachability unknown;
- direct XAPI possible-host and assert-can-boot-here checks;
- Xen Orchestra same-pool heuristic with explicit low-confidence warnings;
- standalone Xen reported as unsupported rather than silently compatible;
- canonical target IDs, encrypted native identity resolution, bounded provider evidence, and sanitized failures;
- lazy common-VM Migration tab with scores, checks, blockers, warnings, and no execute control.

## Validation

- full Jest: 204 suites passed; 2,599 tests passed, 4 skipped, 2,603 total;
- provider contract, registry boundary, API route, UI, Proxmox, vSphere, XO, XAPI, and raw-Xen fixtures: green;
- full ESLint: zero errors, 13 historical warnings;
- JavaScript syntax and `git diff --check`: clean;
- GitHub CI run `30212339638`: success;
- GitHub Docker Build & Push run `30212339639`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-d6e0192`, healthy, zero restarts;
- free root capacity: 511,430,504,448 bytes;
- consistent database backup: `/data/backups/pre-deploy-6e132e9.db`, 2,811,764,736 bytes;
- backup SHA-256: `b420ff3ef77a4bdf92e503176c44d3424c2b750ec169160552869c7330cfc390`;
- backup validation: `quick_check=ok`, zero foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-6e132e9/.env`;
- immutable source: `/home/localadmin-a/docker-dash/releases/6e132e9`;
- source archive SHA-256: `a7bbfb0566b7edcf1f92ff9cdf76dff6e0887f92407531913de371bdcaa61c5e`.

Deployment evidence:

- image: `docker-dash:8.21.4-6e132e9`;
- image ID: `sha256:5c55b6e464df1b0e200d1e3941466b5c11fb877abce536c55ee575fce33bebcd`;
- OCI revision: exact functional commit;
- app: running, healthy, zero restarts;
- persistent database, stacks, certificates, egress policy, and read-only Docker socket mounts preserved.

Smoke evidence:

- `/api/health`: HTTP 200;
- common VM page JavaScript: HTTP 200 and contains the migration-preflight rendering path;
- unauthenticated migration-preflight endpoint: HTTP 401;
- all five new migration capability keys load from the deployed image;
- live database: `quick_check=ok`, zero foreign-key violations;
- zero fatal, uncaught, unhandled, migration-failure, or corruption log matches.

No provider credential was exposed and no real migration, reservation, or provider mutation was attempted.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected before authentication because the ED25519 host key still differs from `known_hosts` line 8. The presented fingerprint is:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The key was not accepted, replaced, deleted, or bypassed. Public deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-d6e0192` with `/home/localadmin-a/docker-dash/deploy-backups/env-before-6e132e9/.env`. V2.1 adds no schema migration. The verified database backup is retained only for explicit disaster recovery and must not be restored automatically over a live database.
