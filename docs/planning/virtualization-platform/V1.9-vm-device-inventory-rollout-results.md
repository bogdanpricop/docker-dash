# V1.9 VM device inventory — rollout results

**Functional commit:** `d6e0192a9397638ae4eafeb395e42deb27e22491`

## Delivered

- uniform, bounded disk and NIC contract with deterministic opaque device IDs;
- live Proxmox QEMU/LXC configuration and optional guest-agent addresses;
- vSphere controller, disk backing, datastore, distributed-network, and VMware Tools correlation;
- Xen Orchestra VBD/VDI/SR and VIF/network reads;
- direct XAPI VM/VBD/VDI/SR/VIF/network/guest-metrics reads;
- standalone `xl` and legacy `xm` runtime topology with explicit incomplete-data warnings;
- tri-state hot-plug/hot-unplug/online-resize/connect-disconnect evidence;
- common VM detail disk and NIC tables with topology totals;
- bounded Xen read concurrency (eight devices per worker pool) and partial-failure isolation.

No disk or network mutation was enabled. Attach, detach, resize, move, delete, VLAN, trunk, and overlay operations remain behind the V4.1/V4.3 safety design.

## Validation

- full Jest: 202 suites passed; 2,582 tests passed, 4 skipped, 2,586 total;
- new common-contract, registry, UI, Proxmox, vSphere, Xen Orchestra, XAPI, and raw Xen fixtures: green;
- full ESLint: zero errors, 13 historical warnings;
- JavaScript syntax and `git diff --check`: clean;
- GitHub CI run `30211311412`: success;
- GitHub Docker Build & Push run `30211311440`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-f7fde38`, healthy, zero restarts;
- free root capacity: 518,323,011,584 bytes;
- consistent database backup: `/data/backups/pre-deploy-d6e0192.db`, 2,809,827,328 bytes;
- backup SHA-256: `211a16b65c0cb000159cb5df8aabf5dcb4d9f5a3e3f9e24d49fe9abc1695e413`;
- backup validation: `quick_check=ok`, zero foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-d6e0192/.env`;
- immutable source: `/home/localadmin-a/docker-dash/releases/d6e0192`;
- source archive SHA-256: `cbed15be23a438a57ab6c371413a505065c91c3487d7bd2186df0c6c236c7977`.

Deployment evidence:

- image: `docker-dash:8.21.4-d6e0192`;
- image ID: `sha256:7edb097e0985ec32474f38caf8a2b5d59117eec3969f65445c5e9a0fa10448eb`;
- OCI revision: exact functional commit;
- app: running, healthy, zero restarts;
- persistent database, stacks, certificates, egress policy, and read-only Docker socket mounts preserved.

Smoke evidence:

- `/api/health`: HTTP 200;
- VM page JavaScript: HTTP 200 and contains the new disk-capacity and connect/disconnect render paths;
- unauthenticated VM detail: HTTP 401;
- live database: `quick_check=ok`, zero foreign-key violations;
- all four new capability keys load from the production image;
- zero fatal, uncaught, unhandled, migration-failure, or corruption log matches.

No provider credential was exposed and no real VM device mutation was attempted.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected before authentication because the ED25519 host key still differs from `known_hosts` line 8. The presented fingerprint is:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The key was not accepted, replaced, deleted, or bypassed. Public deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-f7fde38` with `/home/localadmin-a/docker-dash/deploy-backups/env-before-d6e0192/.env`. V1.9 adds no schema migration. The verified database backup is retained only for explicit disaster recovery and must not be restored automatically over a live database.
