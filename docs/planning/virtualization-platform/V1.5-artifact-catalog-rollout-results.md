# V1.5 Provider artifact catalog — rollout results

**Final commit:** `0d88227b098399e0d15b06d149a6938052b98dd1`

**Functional commit:** `7e2ba90db277872ab09e2127cf8dc16a77c778d1`

**Date:** 2026-07-26

## Local and CI evidence

- 189 Jest suites passed;
- 2,520 tests passed, 4 skipped / 2,524 total;
- ESLint: 0 errors, 13 pre-existing warnings;
- final CI run `30205807250`: success;
- final Docker Build run `30205807254`: success;
- draft PR: #13, branch `agent/advanced-compose-gitops`.

## Staging preflight and backups

Target: `localadmin-a@192.168.13.20`, strict host-key checking.

- source image: `docker-dash:8.21.4-0265a54`, healthy, restarts 0;
- active operations: 0; unknown operations: 0; active snapshot-policy runs: 0;
- pre-feature backup: `/data/backups/pre-deploy-7e2ba90.db`;
- final patch backup: `/data/backups/pre-deploy-0d88227.db`, 2,743,336,960 bytes;
- both backup and live DB: `integrity_check=ok`, foreign-key violations 0;
- config snapshots:
  - `/home/localadmin-a/docker-dash/deploy-backups/env-before-7e2ba90/`;
  - `/home/localadmin-a/docker-dash/deploy-backups/env-before-0d88227/`.

## Deployment

- immutable releases:
  - `/home/localadmin-a/docker-dash/releases/7e2ba90`;
  - `/home/localadmin-a/docker-dash/releases/0d88227`;
- final image: `docker-dash:8.21.4-0d88227`;
- final state: healthy, restart count 0;
- migration `112_provider_artifact_catalog.js`: applied once;
- catalog rows after probe: 11.

## Live ESXi evidence

Read-only probe against active vSphere host #5:

- 11 artifacts, all `iso` on this endpoint;
- 11/11 canonical IDs match `dda_art_[a-f0-9]{26}`;
- no native datastore path, MoRef, OpaqueRef or `nativeRef` in the public envelope;
- encrypted DB columns contain no plaintext native-reference pattern;
- 12 independent catalog processes succeeded consecutively under normal telemetry writes;
- unauthenticated artifact endpoint: HTTP 401;
- catalog JS asset: HTTP 200 and index hook present;
- fatal/error log scan after final rollout: 0/0;
- active and unknown operations remained 0.

## Rollout finding and correction

The first image exposed an intermittent `SQLITE_BUSY_SNAPSHOT` while the catalog transaction raced with telemetry writers in SQLite WAL mode. The exact code was reproduced after nine independent probes. Commit `0d88227` retries the whole catalog transaction at most five times only for `SQLITE_BUSY*`; constraints and all other errors remain fail-closed. The dedicated regression test, full suite, CI and 12/12 live repeatability probe passed after the correction.

## Public VPS

Deployment remains blocked before authentication because strict SSH verification reports a changed ED25519 key for `[89.37.212.66]:2224`:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The stored offending entry is `known_hosts` line 8. No key was replaced and strict checking was not bypassed.

## Rollback

The known-good pre-feature rollback target is image `docker-dash:8.21.4-0265a54` plus `/home/localadmin-a/docker-dash/deploy-backups/env-before-7e2ba90/.env`. Migration 112 is additive and ignored by the older image. A data rollback, only if explicitly required, uses the verified pre-feature DB backup after stopping the app and preserving the current DB separately.
