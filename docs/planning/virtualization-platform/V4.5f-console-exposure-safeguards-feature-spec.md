# V4.5f — Console exposure safeguard evidence

**Release target:** v8.41.0 (2026-07-29)

Reports declared console-gateway controls from the SDK capability contract:
single-use token, server-side credential isolation, emergency lock, protocol and
client metadata. It is not a TLS, network exposure, authorization, or active
console validation and cannot open a console or disclose credentials.
