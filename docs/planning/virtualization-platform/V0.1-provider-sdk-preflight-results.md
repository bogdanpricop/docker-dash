# V0.1 Preflight și validation results

**Data:** 26 iulie 2026  
**Batch:** V0.1 Provider SDK v2  
**Status implementare:** validat local, publicat și deployat pe staging

## Assumption audit

| ID | Rezultat | Evidence |
|---|---|---|
| A01 | confirmat | `docker_hosts.daemon_type` este domeniul și dispatch-ul canonic |
| A02 | confirmat | route-ul folosește `requireHostAccess('view', { param: 'hostId' })`; route tests verzi |
| A03 | confirmat prin client/fixture | Proxmox token probe folosește read-only `/version`, fără CSRF |
| A04 | confirmat cu fixtures | vCenter/ESXi sunt deduse din ServiceContent; altfel `unknown` |
| A05 | confirmat prin review | Xen `info()` și `capabilities()` sunt read-only |
| A06 | confirmat | invalidare la host update/delete/common reconnect, Xen și vSphere reconnect |
| A07 | confirmat | `/api/system/info` nu și-a schimbat schema; full suite verde |
| A08 | acceptat | 60s success TTL, 10s failure TTL, admin refresh și invalidare explicită |
| A09 | confirmat | catalog complet sub limita hard de 256 KiB |
| A10 | confirmat | raw errors sunt transformate în cod/mesaj bounded; secret-leak regression test verde |
| A11 | confirmat | suita completă rulează local |
| A12 | confirmat | staging build/deploy a funcționat pentru V0.0 |
| A13 | blocat extern | fingerprint-ul SSH al VPS-ului diferă de cheia cunoscută; strict checking rămâne activ |

## Implementare

- catalog neutru și aditiv cu 29 capability keys;
- schema `1.0`, complete evidence și validare bounded;
- registry cu 60s/10s cache, maximum 512 entries și in-flight dedup;
- adaptoare Proxmox, vSphere și Xen;
- endpoint host-scoped `GET /api/providers/:hostId/capabilities`;
- refresh admin-only și audit `provider_capability_refresh`;
- cache invalidation pe lifecycle/reconnect;
- Prometheus counters/gauge fără labels de endpoint;
- feature flag `DD_PROVIDER_SDK_V2`;
- documentație runtime și `.env.example`.

## Teste

- targeted Provider SDK + metrics: 5 suite, 37 teste, toate trecute;
- full suite: 168 suite trecute;
- 2.378 teste trecute, 4 skipped, 2.382 total;
- ESLint: 0 errors, 13 warnings preexistente;
- `git diff --check`: curat.

## Deploy gates rămase

- VPS deploy numai după verificarea out-of-band a fingerprint-ului ED25519.

## Rezultat deploy staging

- commit: `8ad9d04`;
- imagine: `docker-dash:8.21.4-8ad9d04`;
- container health: `healthy`, restart count `0`;
- `/api/health`: `ok`;
- route mount/auth smoke: `/api/providers/1/capabilities` returnează `401` fără sesiune;
- schema runtime: `1.0`, 29 feature keys, validare reușită;
- live provider evidence pe endpoint-ul ESXi de staging: `reachable`, variant `esxi`, produs `VMware ESXi 8.0.2`, 4 funcții implementate marcate `supported` și 25 marcate explicit `unsupported`;
- backup DB și `.env` anterior păstrate înainte de recreate.
