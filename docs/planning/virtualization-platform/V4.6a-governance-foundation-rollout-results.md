# V4.6a — Governance foundation rollout results

**Release:** v8.49.0 (`b2a81a1`) — 2026-07-29

V4.6a delivered B176–B185 as an additive governance control plane over the
existing tenant seam. The release was pushed to
`agent/advanced-compose-gitops`, tagged `v8.49.0`, built from the immutable
commit archive and deployed to both production targets.

## Verification

- ESLint: 0 errors; 14 pre-existing warnings outside this batch.
- Jest: 258 suites passed; 2,830 tests passed and 4 skipped.
- Governance tests: 4 suites and 15 tests passed.
- Commit archive: 19,149,419 bytes, SHA-256
  `241e6209579b7d5b7a6e2bf8248ff52af16007341aacee08d0a43d98fe44bc6a`.
- Both images were built with `--target production` and report OCI application
  version `8.49.0`.
- The local workstation has no Docker CLI, so runtime verification was executed
  on the two SSH-key targets.

| Target | Runtime | Verified backup | Governance schema |
|---|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.49.0-b2a81a1`, healthy, 0 restarts, `/api/health` = `8.49.0` | `backups/v46a-20260729T150629Z-b2a81a1/docker-dash.db`, 3,698,589,696 bytes, mode `600`, SHA-256 `701f4075fdce39f53e5b3d2f8660acd13278120d500e1d973f9aa2be26ed5ecd` | 12 permissions, 4 built-in roles, 2 initial scopes |
| VPS `89.37.212.66` | `docker-dash:8.49.0-b2a81a1`, healthy, 0 restarts, `/api/health` = `8.49.0` | `backups/v46a-20260729T150629Z-b2a81a1/docker-dash.db`, 435,871,744 bytes, mode `600`, SHA-256 `e3454019cf257ef786d86280530cee6e4aedc75c6124f429a450e64d4ca7af13` | 12 permissions, 4 built-in roles, 2 initial scopes |

SQLite `integrity_check` returned `ok` for both backups before container
replacement. The externally served Governance JavaScript asset and health
endpoint were also verified on both targets.

The first Compose invocation stopped before replacement because service
`env_file` was resolved relative to the immutable release directory. The old
v8.48.0 containers remained healthy. Deployment then succeeded with the base
directory supplied explicitly through `--project-directory`; no database
restore or provider mutation was needed.

## Safety and rollback

Resource assignments are accounting metadata only. No Docker/provider resource
is moved or modified, raw invitation tokens are never persisted, and hard quota
failure rolls back the assignment transaction.

Application rollback uses `docker-dash:8.48.0-ef41e76` with its immutable
release compose file. Migration 124 is additive and the earlier application
ignores its tables. Database backups are retained for explicit disaster
recovery only and must not be restored automatically over a live database.
