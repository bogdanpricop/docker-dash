# V0.3 Preflight și validation results

**Data:** 26 iulie 2026

**Batch:** V0.3 Durable provider operation core

**Status implementare:** validat local; push și deploy staging în curs

## Implementare

- migrarea aditivă 107 cu operations, events, resource locks și control policies;
- state machine persistent și Activity Center API schema `1.0`;
- encrypted request/native task bridge și idempotency conflict detection;
- atomic multi-resource locks, owner lease, heartbeat și restart recovery;
- timeout→reconcile, transient retry bounded și unknown-state workflow;
- cancel confirmat de handler, fără false-success;
- global/provider/host read-only, emergency stop și freeze windows;
- RBAC + host access pentru list/detail/events/cancel/manual resolution;
- WS best-effort cu DB ca source of truth;
- startup/shutdown simetric și worker configurabil/bounded.

## Verificări locale

- targeted: 4 suite, 20 teste, toate trecute;
- full suite: 174 suite trecute;
- 2.418 teste trecute, 4 skipped, 2.422 total;
- fresh-schema run a aplicat migrarea 107;
- two-engine claim a executat handler-ul exact o dată;
- lock conflict, lease recovery, timeout, retry și cancel paths validate;
- ciphertext, API omission, redaction și idempotency conflict validate;
- dynamic `writeable` gate și policy matrix validate;
- legacy ACME/remediation/migration/procedure tests au rămas verzi.

## Deploy gates

- staging: backup DB + `.env`, image SHA, migration, worker/route/health smoke;
- VPS: blocat până la confirmarea out-of-band a noii amprente ED25519;
- rollback: V0.2 ignoră tabelele 107; operațiile rămân pentru reconciliere la revenire.
