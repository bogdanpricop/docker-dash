# V2.6 placement and HA mutation — rollout results

## Release identity

- functional commit: `1b25b7cccb6a55a0306325becd7e4f23159a33bd`;
- branch / review: `agent/advanced-compose-gitops`, draft PR #13;
- CI: run `30219648805` passed;
- Docker build: run `30219648822` passed;
- staging image: `docker-dash:8.21.4-1b25b7c`;
- image ID: `sha256:986da1dbb35abfdd12e183e0c30f6b86cbd915b91733f18b228249bc61adfcde`;
- OCI revision: `1b25b7cccb6a55a0306325becd7e4f23159a33bd`;
- immutable release: `/home/localadmin-a/docker-dash/releases/1b25b7c`.

## Validation

- full repository: 216 suites passed, 2,679 tests passed and 4 explicitly skipped;
- focused post-audit regressions: 42 and 35 tests passed respectively;
- lint: zero errors and 13 pre-existing warnings;
- `git diff --check`: clean;
- GitHub CI passed production critical-vulnerability, backend/frontend syntax, lint, tests and i18n gates;
- Docker Build & Push passed for the exact functional SHA;
- migration `116_provider_placement_changes.js` is recorded and all three placement-change tables exist.

The in-app browser skill could not run visual QA because this session exposed no browser instance. The page is instead covered by DOM/XSS tests, the complete repository suite, static-asset smoke and authenticated staging API smoke.

## Backup and transfer evidence

- online SQLite backup: `/home/localadmin-a/docker-dash/backups/v26-20260726T204806Z-1b25b7c/docker-dash.db`;
- backup bytes: `2,902,749,184`;
- backup SHA-256: `9e8024dc2d06f0413d22de7a8af0c0c0021f196d66cc481152c3ee49a65fdc28`;
- backup `quick_check`: `ok`; foreign-key violations: `0`;
- configuration and previous Compose file are stored in the same mode-700 backup directory;
- exact Git archive bytes: `18,864,324`;
- exact Git archive SHA-256: `bacd05de8c0c3fe3dffbae59bae6ddfbc848df6129cd53261f80b759a5f082c5`;
- archive checksum matched before extraction.

## Staging smoke

- target: `localadmin-a@192.168.13.20`, strict known-host verification;
- container: healthy, restart count `0`;
- `/api/health`: HTTP 200, `status=ok`, version `8.21.4`;
- unauthenticated placement-change list: HTTP 401;
- Placement Advisor JavaScript asset: HTTP 200;
- authenticated placement-change list: HTTP 200, schema `1.0`;
- invalid authenticated preflight: controlled HTTP 400 with `INVALID_PLACEMENT_CHANGE_KIND`;
- endpoint 5 live capability probe: standalone ESXi 8.0.2, HA policy mutation, affinity mutation and rebalance apply all `unsupported`;
- all three V2.6 mutation flags are explicitly `false` in the runtime environment;
- no placement request or provider mutation was created during smoke;
- temporary smoke-session count after cleanup: `0`;
- live DB after smoke: `quick_check=ok`, foreign-key violations `0`;
- the only log matches contained `errorCount=0`; no fatal, unhandled or migration-error event was present.

Standalone ESXi has no cluster management boundary, so the three mutation capabilities must remain unsupported. Keeping flags off and observing a provider-specific unsupported result is the expected fail-closed rollout outcome.

## VPS gate

Strict SSH verification stopped before authentication on `root@89.37.212.66:2224`:

- pinned ED25519: `SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`;
- presented ED25519: `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`.

No known-host entry was altered and no bypass was used. VPS deployment remains blocked until the presented key is verified out-of-band.

## Rollback

- previous image: `docker-dash:8.21.4-ea1a203`;
- restore `.env` from the V2.6 backup directory;
- run Compose with project directory `/home/localadmin-a/docker-dash`, project `docker-dash`, and `/home/localadmin-a/docker-dash/releases/ea1a203/docker-compose.yml` using `--no-build --no-deps app`;
- migration 116 is additive and the previous application ignores its tables, so an application rollback does not normally restore the database;
- the verified online database backup is reserved for incident recovery where schema/data restoration is explicitly required.

