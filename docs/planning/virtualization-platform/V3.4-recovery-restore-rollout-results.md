# V3.4 — Recovery restore rollout results

## Outcome

V3.4 was implemented in functional commit `75c1fe209c693b4e02724b4f6d7de41800a01b59`, pushed to `agent/advanced-compose-gitops`, and deployed to staging at `192.168.13.20`. The release enables create-only full Proxmox VE QEMU/LXC restore through a durable operation while keeping file, disk, instant, differential, Xen, and vSphere restore fail-closed.

No provider restore mutation was attempted on staging because its provider is ESXi and there is no disposable Proxmox VE/PBS target. Provider-real promotion remains a separate canary gate. The public VPS was not changed because strict SSH verification detected a different ED25519 host key.

## Validation evidence

- targeted V3.4 suites: 106 tests passed; the override-reason follow-up set passed 60 tests;
- local full suite: 226 suites passed, 2,729 tests passed and 4 skipped (2,733 total);
- GitHub CI run `30224096966` exposed one unrelated probabilistic crypto-test no-op and correctly failed;
- follow-up commit `da62ceb9cebde207231983e01d2699547f5b9a2c` makes the tamper mutation deterministic; 4,096 local trials and the 13-test crypto suite passed;
- final GitHub CI run `30224432844`: 226 suites and 2,729 tests passed, 4 skipped;
- final GitHub Docker Build & Push run `30224432842`: success;
- lint: zero errors and the 13 existing warnings;
- no V3.4 migration was required; the deployed chain remains at `119_provider_backup_execution.js`.

## Staging release

| Evidence | Value |
|---|---|
| Release directory | `/home/localadmin-a/docker-dash/releases/75c1fe2`, mode `555` |
| Image | `docker-dash:8.21.4-75c1fe2` |
| Image ID | `sha256:2894ad23a44c64cb44ef6dab9ae17d93c572fc5ccb91b4efa8d1c9ab351f7fa5` |
| OCI revision | `75c1fe209c693b4e02724b4f6d7de41800a01b59` |
| Source archive | `incoming/docker-dash-75c1fe2.tar.gz`, 18,957,530 bytes |
| Archive SHA-256 | `594d69e2cab94a076c1422412ba6b8309fcec0c3f52bd4d702e1e5a81a0d74f9` |
| SQLite backup | `/home/localadmin-a/docker-dash/backups/v34-20260726T225910Z-75c1fe2/docker-dash.db` |
| Backup size | 2,942,042,112 bytes |
| Backup SHA-256 | `b500e065e044f09a9f6fa852a4625e0a28ef1a426b1c8e1ad3737ce427e9b3e3` |
| Permissions | backup directory `700`, database `600`, release environment `400` |

The backup used SQLite's online backup API and passed `quick_check=ok` before it was copied out of the data volume. Its checksum was revalidated before promotion.

`DD_PROVIDER_RECOVERY_RESTORE=true` is enabled only on staging. Post-deploy evidence:

- container is healthy with zero restarts and exact OCI revision;
- `/api/health`, the application page, and the shipped recovery API client return HTTP 200;
- unauthenticated restore preflight returns 401;
- an authenticated capability request returns 200 and exposes `backup.restore.vm` as unsupported for the ESXi staging provider;
- authenticated preflight for a nonexistent canonical point returns sanitized `404 RECOVERY_POINT_NOT_FOUND`;
- temporary admin smoke session was deleted;
- database `quick_check=ok`, zero foreign-key violations, and latest migration `119_provider_backup_execution.js`;
- recent logs contain no migration, fatal, unhandled, or restore-operation failures.

## Rollback behavior exercised

The first promotion switched to the new healthy image but a smoke assertion searched for an incorrect JavaScript helper name. The remote `ERR` trap restored `docker-dash:8.21.4-c99a314` and the prior environment automatically. Inspection proved the functional endpoint was present; the corrected assertion checked the real `/restore/preflight` route, the backup checksum was revalidated, and promotion then passed every gate. No provider or database mutation occurred during the failed attempt.

Rollback anchors are the immutable `c99a314` release/image and the verified V3.4 online backup. A provider task failure or cancellation never authorizes automatic deletion of a partial restore target.

## VPS gate

Strict SSH to `root@89.37.212.66:2224` remains blocked. The pinned ED25519 fingerprint is `SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`; the server presents `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`. No key was replaced, ignored, or accepted.

## Promotion gate

Before production authorization, use a non-critical Proxmox VE target and a verified PVE/PBS recovery point. Prove target-node/storage/VMID conflict checks, QEMU and LXC UPID reconciliation, stopped post-state, cancellation evidence, ambiguous restart recovery, and manual inspection of a deliberately failed partial target. File and disk restore require separate bounded streaming and destination-ownership contracts.
