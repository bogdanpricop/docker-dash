# V2.5 — Assumption audit

| Assumption | Failure if false | Control |
|---|---|---|
| all plan IDs B201–B225 belong in V2.5 | observability/AIOps swallows the placement batch and is duplicated in V6.4 | implement only B201/B203/B205 placement telemetry subset; retain the rest for named V6.4 |
| every Proxmox release exposes HA rules | PVE 8 returns 404 or misleading empty inventory | version gate PVE 9+, explicit unsupported on older versions |
| every affinity rule is mandatory | optional preferences become false blockers | preserve mandatory/strict semantics and show warnings for preferences |
| Xen VM groups are hard constraints | planner claims guarantees Xen may breach | model anti-affinity as advisory and surface WLB/home-server precedence |
| a VM home server is its current host | source placement and compliance are wrong | keep `affinity` separate from runtime residence |
| vSphere rule status alone proves compliance | API property can be unused or stale | evaluate current canonical placement and retain provider status only as evidence |
| CPU utilization can predict migrated VM demand | target overload estimate is fabricated | score current CPU headroom only; do not project VM CPU consumption |
| configured VM memory equals active demand | target memory estimate is exact | use configured memory + 10% reserve and label conservative |
| shared storage/network inventory proves compatibility | migration fails on device/fabric details | reuse V2.1 provider checks; unknown remains unknown |
| a recommendation score authorizes migration | unsafe automatic rebalance | V2.5 has no apply call; V2.6 must re-preflight, approve and reconcile |
| independent per-VM picks compose safely | several moves overbook one target | rebalance planner carries forward target memory reservations |
| native recommendation text is safe UI content | XSS/secret/provider-detail leakage | sanitize/bound server-side and escape UI output |
| relationship IDs can expose provider refs | topology leaks | canonicalize relations through encrypted identity store |
| all provider data is equally fresh | old metrics look current | explicit observedAt/freshness per dimension and evidence-coverage cap |
