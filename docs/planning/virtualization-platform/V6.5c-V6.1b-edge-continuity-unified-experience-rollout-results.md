# V6.5c / V6.1b — Edge continuity and unified experience rollout

**Release:** v8.67.0

**Feature commit:** `0f486ad`

**Date:** 2026-07-29

## Result

B346–B355 were implemented, tagged `v8.67.0`, pushed to draft PR #13 and
deployed as `docker-dash:8.67.0-0f486ad` to LAN and VPS. Migration 141 is
present exactly once on both databases, with 43 edge tables and 70 governance
permissions.

## Verification

- Full Jest: 278/278 suites passed; 3,036 tests passed and 4 skipped.
- Focused edge/experience/operation UI and API validation: 8 suites and 60
  tests passed.
- ESLint: 0 errors and 14 pre-existing warnings.
- `git diff --check`, JavaScript syntax checks and focused zero-warning lint
  passed before publication.
- GitHub CI run `30501021048` and Docker Build & Push run `30501021065`
  completed successfully for `0f486ad`.
- Exact Git archive: `docker-dash-8.67.0-0f486ad.tar.gz`, 19,585,442 bytes,
  SHA-256 `e0d462162bc7941535ea1d612406336a8303310d8aab61318bda4b3916e15a58`.

## Safe deployment evidence

Both deployments validated the exact archive checksum, copied `.env` into the
backup and compared it byte-for-byte, enforced mode 600, created an online
SQLite backup, and opened that backup read-only with the previous v8.66.0
image under a rootless-network/read-only/tmpfs sandbox. The application was
then rebuilt with the Docker `production` target and recreated with the base
project directory and base `.env`, without rebuilding dependencies in Compose.

| Target | Runtime | Verified backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.67.0-0f486ad`, image ID `sha256:8ec95c3a72c188deb2d9e8fa527da92637fc0c91125ab3241e22fef6fd4e324a`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v65c-20260729T235737Z-0f486ad/docker-dash.db`, 3,770,851,328 bytes, mode 600, SHA-256 `9198742e1c93deec9bd67876b00c67a1ec655e1adb6353a30f812f74d810a0d0` |
| VPS `89.37.212.66` | image `docker-dash:8.67.0-0f486ad`, image ID `sha256:1f5e4458b62862c28c1b57ab519c6eee6901fd0b2bcb6cd58474d316516083a6`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v65c-20260729T235908Z-0f486ad/docker-dash.db`, 443,289,600 bytes, mode 600, SHA-256 `e55fdc43a649d2116444556603296d3c8e3c594ee9a9f8480b79858d76cb3eae` |

The immediate prior release/image `8.66.0-3bbdd18` remains available on both
targets. The exact previous-previous release/image `8.65.0-a4d2d65` and both
incoming archives were removed after verification. Available root filesystem
space after rollout is 451,022,471,168 bytes on LAN and 8,328,687,616 bytes on
VPS.

## Smoke checks

- LAN and VPS `/api/health` report `status=ok`, version `8.67.0` and standalone
  mode; both containers have zero restarts.
- External `/` returns HTTP 200 on both targets.
- Unauthenticated `/api/edge/overview` and `/api/experience/home` return HTTP
  401 on both targets.
- Live SQLite `quick_check` is `ok`; migration 141 count is one, edge-table
  count is 43 and governance permission count is 70.

## Rollback

The immediate rollback is `docker-dash:8.66.0-3bbdd18` with release directory
`v8.66.0-3bbdd18`. The verified pre-migration backups above are compatible
with that image. Use the base project directory and base `.env` and recreate
only `app` with `APP_VERSION=8.66.0-3bbdd18`.
