# V4.6b — Identity and policy governance rollout

**Release:** v8.50.0 (`4b08bd0`) — 2026-07-29

**Backlog:** B186–B195

## Result

V4.6b was committed and pushed on `agent/advanced-compose-gitops`, tagged
`v8.50.0`, and deployed from the immutable Git archive whose SHA-256 is
`2e98951b56abf6f38626652420abb8818e19966ac65bc6cd5206a00f3cbd0c1e`.
The unrelated in-progress stack-sizing changes in the local worktree were not
included in the commit, archive, image or deployment.

Both targets built the production stage and recreated only the `app` service
with project directory and environment anchored to the persistent base path.
No provider resource or sidecar was changed.

| Target | Runtime result | Verified pre-deploy backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.50.0-4b08bd0`, healthy, 0 restarts; `/api/health` = `8.50.0` | `/home/localadmin-a/docker-dash/backups/v46b-20260729T154541Z-4b08bd0/docker-dash.db`, 3,698,589,696 bytes, SHA-256 `684e01e02d8933a920458ce874a82adde631f6a226154ae313a6afdabefc4339` |
| VPS `89.37.212.66` | `docker-dash:8.50.0-4b08bd0`, healthy, 0 restarts; `/api/health` = `8.50.0` | `/opt/docker-dash/backups/v46b-20260729T154729Z-4b08bd0/docker-dash.db`, 435,871,744 bytes, SHA-256 `c72dae7565159c15a49a0a4e6a20d728870517f3ca94376346e193c60bb4f94d` |

Each backup directory also contains the pre-deploy `.env`, compose file and
container inspection snapshot. Sensitive `.env` and SQLite files have mode
`600`.

## Verification

- migration `125_governance_identity_policy.js` applied on both databases;
- 19 governance permissions and 23 `governance_%` tables reported on both;
- external health and public federation discovery endpoints respond on both;
- full Jest run: 259 suites passed, 2,839 tests passed, 4 skipped;
- subsequent V4.6b focused run: 11 tests passed, including RS256, ES256,
  EdDSA, assertion replay, SCIM session invalidation, quota/approval and
  blackout/emergency behavior;
- targeted ESLint and JavaScript syntax checks passed.

## Operational observations

The release introduced no startup error. Existing environment warnings remain:
LAN has CSRF disabled; both HTTP deployments have insecure cookies and seven-day
audit retention. LAN also continues to report invalid credentials for the
pre-existing `Romarg` host entry. These settings/credentials were not changed by
this rollout.

Rollback is a redeploy of the v8.49.0 immutable release. Migration 125 is
additive; the backup above is required only if a data rollback is explicitly
chosen.
