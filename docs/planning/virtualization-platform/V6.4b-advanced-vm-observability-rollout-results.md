# V6.4b — Advanced VM observability operations rollout

**Release:** v8.54.0  
**Feature commit/tag:** `2d80878` / `v8.54.0`  
**Deployed commit:** `4387d9f`  
**Date:** 2026-07-29

## Result

V6.4b was pushed to `agent/advanced-compose-gitops` and deployed from the final
tested commit to both standalone environments. The immutable source archive had
SHA-256 `2d25fae1038a69b2a280caf6cbf89121f352e4d740360958b429b7f8d7889f01`.
The deployment commit adds the complete operator control surface and closes the
research/implementation-plan status on top of the tagged feature commit.

## Pre-deployment verification

- Jest: 263/263 suites passed; 2,877 tests passed and 4 skipped.
- ESLint: 0 errors and 14 pre-existing warnings.
- Dedicated V6.4b suite: all 9 tests passed, including five repeated executions
  used to check deterministic sampling and time-sensitive SLO behavior.
- JavaScript syntax checks and `git diff --check` passed.

## Deployment evidence

| Environment | Runtime | Verified online SQLite backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.54.0-4387d9f`, healthy, 0 restarts | `/home/localadmin-a/docker-dash/backups/v64b-20260729T180227Z-4387d9f/docker-dash.db`, 3,713,851,392 bytes, SHA-256 `e7f9b1ef7b646903c7dd7532eea7724b1f4c414dd508868182e35bb3bd96ccb7` |
| VPS `89.37.212.66` | `docker-dash:8.54.0-4387d9f`, healthy, 0 restarts | `/opt/docker-dash/backups/v64b-20260729T180531Z-4387d9f/docker-dash.db`, 435,871,744 bytes, SHA-256 `a92cf66227dd26761476c16eba61e068f1f27258813bac772326b53b0ea2f95b` |

Each backup passed SQLite `quick_check` and returned zero foreign-key
violations before its environment was rebuilt. Environment snapshots, rendered
Compose config and pre-rollout container inspection are stored beside each
database with private permissions.

The first LAN verification attempt could not open a bind-mounted backup inside
a private host directory. It stopped before build or container recreation. The
same unchanged copy was then verified by copying it into an isolated temporary
container, preserving the private directory permissions.

## Post-deployment checks

- `/api/health` returned version `8.54.0` on LAN and VPS.
- Migration `128_vm_observability_operations.js` is recorded once.
- SQLite `quick_check` is `ok`; governance permissions = 29 and
  `vm_observability_%` tables = 16.
- External `/` smoke returned HTTP 200; unauthenticated advanced-observability
  API access returned HTTP 401 on both environments.
- Deployment-window log scan found no migration failure, uncaught exception or
  fatal entry.
- VPS no longer has mutable `/app/src` or `/app/public` bind mounts; explicit
  release Compose recreated the app from the immutable image while retaining
  only the declared data, socket and stack mounts.
- Rollback remained available to the inspected previous image but was not
  needed.

## Safety boundary retained

Forecasts and root-cause candidates remain advisory. Export delivery is
explicit, bounded, audited and residency checked. Telemetry retention requires
an exact typed confirmation and no provider operation is inferred from metric
or event evidence.
