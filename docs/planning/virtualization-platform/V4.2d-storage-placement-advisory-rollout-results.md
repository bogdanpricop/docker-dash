# V4.2d — Storage placement advisory rollout results

**Release:** v8.26.0 (`f7bdab4`) — 2026-07-28

## Validation

- focused placement-advisory, storage-posture, adapter and route tests: 65 passed;
- full suite: 241 suites passed, 2,786 tests passed, 4 skipped (2,790 total);
- ESLint: 0 errors; production images were built with `docker build --target production`
  on deployment targets because a local Docker CLI is unavailable.

## Deployment

| Target | Release image | Backup | Result |
|---|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.26.0-f7bdab4` | `backups/v42d-20260728T164103Z-f7bdab4/docker-dash.db` SHA-256 `7088a941acf00c9343e16a90e528b2aa29bfeab0916f41de141ec50d796294de` | healthy |
| VPS `89.37.212.66` | `docker-dash:8.26.0-f7bdab4` | `backups/v42d-20260728T164251Z-f7bdab4/docker-dash.db` SHA-256 `09fbb91be6fc55023c7e6052abdeab726a1f13bdfa73f1f2def7b9136d5b26ca` | healthy |

Both targets returned `/api/health` version `8.26.0`. The SSH-key rollout rebuilt
only the application image and used Compose `--no-deps`, preserving current
services and release flags. No storage reservation, provider operation or disk
mutation was submitted.

## Deferred live smoke

The advisory endpoint needs an authenticated host-view session and a reachable
provider endpoint. It was not used to select, reserve or mutate a storage
target during rollout. A future VM disk action must still obtain its own fresh,
authorized, hash-bound execution preflight.
