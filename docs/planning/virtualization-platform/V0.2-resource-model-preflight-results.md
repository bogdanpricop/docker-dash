# V0.2 Preflight și validation results

**Data:** 26 iulie 2026

**Batch:** V0.2 Resource identity și common resource models

**Status implementare:** validat local; push și deploy staging în curs

## Implementare

- schema comună `1.0` pentru VM, host, cluster, storage, network și task;
- migrarea aditivă 106 și identity store cu ID-uri canonice deterministe;
- AES-256-GCM pentru native refs și SHA-256 pentru lookup fără plaintext;
- stabilitate `stable`, `derived` sau `transient`, inclusiv raw Xen;
- normalizare bounded pentru spec/status/actions/labels/relationships/extensions;
- dispatch read-only prin adaptoarele Proxmox, vSphere și Xen;
- capability gating înainte de orice inventory read;
- limită implicită 200, maximum 500, sortare și truncation deterministe;
- endpoint host-scoped `GET /api/providers/:hostId/resources/:kind`;
- cleanup pentru agenții one-shot și rollback tranzacțional al identity mappings.

## Verificări locale

- targeted: 6 suite, 37 teste, toate trecute;
- full suite: 170 suite trecute;
- 2.398 teste trecute, 4 skipped, 2.402 total;
- migrarea 106 a fost aplicată în full fresh-schema run;
- teste dedicate pentru ciphertext-at-rest, stable UUID/ref change și cascade delete;
- serialization regression pentru toate cele șase kind-uri și refs XAPI/vSphere;
- limit/truncation, capability gating, route RBAC middleware și safe errors acoperite;
- ESLint: 0 errors, 13 warnings preexistente;
- `git diff --check`: curat.

## Deploy gates

- staging: backup DB și `.env`, build cu tag SHA, health și live ESXi inventory probe;
- VPS: blocat până la confirmarea out-of-band a noii amprente ED25519;
- rollback: imaginea V0.1 ignoră tabelul aditiv 106; backup-ul DB rămâne disponibil.
