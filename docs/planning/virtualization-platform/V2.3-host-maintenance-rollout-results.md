# V2.3 host maintenance orchestration — rollout results

**Functional commit:** `27e14e8ac31cc322b6037b1cb4c1fb6e45ebc2ff`

**Final deployed commit:** `547ae12723c989616b169678207272e810ae96f5`

**Stabilization commits:** `aa1e7f977e949f469888739102fcda17e4cea4f4`, `547ae12723c989616b169678207272e810ae96f5`

## Delivered

- persistent host-maintenance runs, items and events with leases, active-host reservations, encrypted plans/native task references and hashed idempotency keys;
- deterministic read-only planning, aggregate memory budgets with a 10% reserve, bounded evidence collection, wave sizes from 1 to 10 and a 200-VM blast-radius ceiling;
- durable child `vm.migrate` operations with immediate re-preflight, bounded waves, pause/resume/cancel and no automatic replay of unknown outcomes;
- stable `drained` and `maintenance` reservations until explicit exit;
- native vSphere `EnterMaintenanceMode_Task` and `ExitMaintenanceMode_Task` reconciliation;
- native Xen XAPI disable/enable tasks with live host-state and workload verification;
- explicit Proxmox drain-only support and fail-closed Xen Orchestra/raw-Xen behavior;
- migration target exclusion while a host is reserved for maintenance;
- admin-only, audited planning and controls plus read-only run visibility and Activity child-operation links;
- release gate `DD_PROVIDER_HOST_MAINTENANCE`, disabled by default and explicitly enabled on staging;
- additive migration `114_provider_host_maintenance.js` and a leader-gated maintenance reconciler;
- bounded retry for common inventory persistence after `SQLITE_BUSY_SNAPSHOT`, discovered during live rollout validation;
- evidence-honest UI/API output that does not claim an empty host when placement is unknown.

## Validation

- focused V2.3/provider regression before publish: 9 suites and 109 tests passed;
- full Jest before publish: 209 suites passed, 2,627 tests passed, 4 skipped, 2,631 total;
- full ESLint: zero errors;
- post-smoke concurrency regression: 4 suites and 45 tests passed;
- final evidence-message regression: 2 suites and 17 tests passed;
- JavaScript syntax checks and `git diff --check`: clean;
- final GitHub CI run `30215413450`: success;
- final GitHub Docker Build & Push run `30215413443`: success;
- draft PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13).

## Staging deploy — 192.168.13.20

Strict SSH host-key verification succeeded for `localadmin-a@192.168.13.20`.

Pre-deploy evidence:

- previous image: `docker-dash:8.21.4-da93c09`, healthy, zero restarts;
- free root capacity: 503,399,718,912 bytes;
- consistent database backup: `/data/backups/pre-deploy-27e14e8.db`, 2,849,394,688 bytes;
- backup SHA-256: `d8149d439289325dd940297514680c8350cb48568945ee2681c7443f1141ff8e`;
- backup validation: `quick_check=ok`, zero foreign-key violations;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-27e14e8/.env`;
- immutable final source: `/home/localadmin-a/docker-dash/releases/547ae12`;
- final source archive: 18,783,154 bytes, SHA-256 `0e08d4401f5f5755ca302e90038b3c93d5b5aaf256eb13ecd19656d58e8be036`.

Deployment evidence:

- image: `docker-dash:8.21.4-547ae12`;
- image ID: `sha256:55756f4cc6967ec0fae8a6f57fe6cc72ca4d55ab26ad5ba93cb4cb93569245bb`;
- OCI revision: exact final deployed commit;
- app: running, healthy, zero restarts;
- persistent database, stacks, certificates, egress policy and read-only Docker socket mounts preserved;
- `DD_PROVIDER_VM_MIGRATION=true` and `DD_PROVIDER_HOST_MAINTENANCE=true` are active in the container.

Smoke evidence:

- `/api/health`: HTTP 200;
- maintenance run and preflight endpoints: HTTP 401 without authentication;
- VM page and API JavaScript: HTTP 200 with maintenance planning/control wiring;
- migration 114 is recorded and all three maintenance tables exist;
- live database: `quick_check=ok`, zero foreign-key violations and zero maintenance runs created;
- zero fatal, uncaught, unhandled, migration-failure or corruption log matches;
- live ESXi read-only preflight observed one running host and five VMs;
- vSphere maintenance capability was `conditional`;
- the plan was correctly blocked with `SOURCE_PLACEMENT_UNKNOWN`, a 64-character semantic plan hash and no false empty-host warning.

No VM migration, host state change or other provider mutation was attempted during rollout.

## Public VPS — blocked safely

The strict connection to `root@89.37.212.66:2224` was rejected before authentication. The expected ED25519 fingerprint from `known_hosts` line 8 is:

`SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`

The server currently presents:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

The key was not accepted, replaced, deleted or bypassed. Public deployment requires independent fingerprint confirmation.

## Rollback

Application rollback uses `docker-dash:8.21.4-da93c09` and `/home/localadmin-a/docker-dash/deploy-backups/env-before-27e14e8/.env`. Migration 114 is additive and old code ignores its tables, so the database must not be restored automatically. The verified database backup is retained for explicit disaster recovery only.
