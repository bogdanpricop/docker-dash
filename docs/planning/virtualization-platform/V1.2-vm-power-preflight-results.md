# V1.2 Preflight results — Safe VM power

Data: 2026-07-26
Commit funcțional: `b593c59`

## Verificare locală și CI

- 182 suite Jest trecute;
- 2.472 teste trecute, 4 skipped, 2.476 total;
- targeted V1.2: 9 suite, 83 teste trecute;
- ESLint: 0 erori, 13 warnings preexistente;
- `git diff --check`: clean;
- GitHub CI run `30202042020`: success;
- Docker Build & Push run `30202042046`: success;
- browser render smoke indisponibil: sesiunea nu a expus nicio instanță in-app Browser; nu a fost înlocuit cu un controller neautorizat.

## Staging `192.168.13.20`

- release: `/home/localadmin-a/docker-dash/releases/b593c59`;
- image: `docker-dash:8.21.4-b593c59`;
- container: healthy, 0 restarts;
- DB backup consistent: `/data/backups/pre-deploy-b593c59.db`, 2.715.480.064 bytes;
- backup `integrity_check=ok`, `foreign_key_check=0`;
- env backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-b593c59/.env`;
- `DD_PROVIDER_VM_POWER=true` activ numai după backup;
- health și asset frontend: OK;
- unauthenticated preflight și submit: ambele `401`;
- scan fatal/migration errors în logurile post-deploy: clean.

## ESXi live read-only

- endpoint host ID 5;
- 5/5 VM-uri normalizate;
- VM verificată: ID canonic, identitate `stable`, state `running`;
- handler durable `vm.power` înregistrat;
- feature gate activ;
- toate cele cinci action plans au hash SHA-256 de 64 caractere;
- start blocat corect de current state/resource action;
- clean și force plans disponibile conform state/tools evidence;
- force plans cer `typed_name`;
- `foreign_key_check=0` după refresh/preflight;
- nicio mutation nu a fost trimisă: nu există o VM disposable desemnată.

## VPS public

Deploy-ul nu a fost executat. Strict host-key checking a respins conexiunea la `89.37.212.66:2224`: endpoint-ul prezintă ED25519 `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`, diferit de cheia cunoscută local (offending entry `known_hosts:8`). Cheia nu a fost acceptată și `known_hosts` nu a fost modificat.

## Rollback

Rollback-ul aplicației folosește imaginea anterioară `docker-dash:8.21.4-549ba98` și env backup-ul de mai sus. V1.2 nu adaugă migrare DB. Operațiile `running`, `reconciling` sau `unknown` trebuie verificate înainte de rollback; în smoke nu a fost creată nicio operație.
