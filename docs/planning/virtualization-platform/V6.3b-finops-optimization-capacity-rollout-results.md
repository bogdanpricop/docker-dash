# V6.3b — FinOps optimization and capacity rollout

**Release:** v8.61.0
**Feature commit:** `e08c40c`
**Date:** 2026-07-29

## Outcome

V6.3b was pushed to `agent/advanced-compose-gitops`, tagged `v8.61.0` and
deployed as `docker-dash:8.61.0-e08c40c` to the LAN and VPS targets. Both app
containers are healthy with zero restarts and five mounts. No optional profile
service was recreated.

## Verification before publish

- Full Jest run: 270/270 suites passed; 2,961 tests passed and 4 skipped
  (2,965 total).
- Dedicated FinOps and governance run: 30/30 tests passed.
- ESLint: 0 errors; 14 pre-existing warnings.
- Browser/API JavaScript syntax passed.
- Release source archive: 19,433,552 bytes; SHA-256
  `b09ac93513b881716faa2130cca00bb61cf503938da65b67e2dcbfc5a6f80c18`.

## Backups and deployments

| Target | Deployment | Verified online SQLite backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.61.0-e08c40c`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v63b-20260729T210301Z-e08c40c/docker-dash.db`, 3,746,947,072 bytes, SHA-256 `4305f2a50b620baa948fef66185744631b141d0bfda45a29b415af47ca6d5509` |
| VPS `89.37.212.66` | `docker-dash:8.61.0-e08c40c`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v63b-20260729T210502Z-e08c40c/docker-dash.db`, 438,198,272 bytes, SHA-256 `0f71ec7dc33d551d09681b1de7af3d537b00f235430ca57cb107ed55f4489187` |

Each backup used the running application's online `better-sqlite3` backup API,
was copied outside the volume with mode 0600 and was mounted as an individual
read-only file into the previous production image for `PRAGMA quick_check`.
The validator ran without network access, with a read-only root filesystem and
an ephemeral writable directory for SQLite lock metadata. A private env copy
and container inspection are retained alongside each database.

## Deployment and rollback method

Both hosts checksummed and extracted the same Git archive, then built the
production target with `APP_VERSION=8.61.0`. Only the app was recreated:

```sh
APP_VERSION=8.61.0-e08c40c docker compose \
  --project-directory "$BASE" --env-file "$BASE/.env" \
  -f "$RELEASE/docker-compose.yml" up -d --no-build --no-deps app
```

The verification scripts retained an app-only rollback to V6.3a. All checks
passed and no rollback was required.

## Post-deploy evidence

- Internal and external `/api/health` report `8.61.0`; `/` returns HTTP 200.
- Unauthenticated `/api/governance/lifecycle/finops/optimization` returns HTTP
  401.
- Migration `135_finops_optimization_capacity.js` is applied exactly once.
- All 11 exact optimization/capacity tables exist and the permission catalog
  contains 47 entries.
- Live and backup `PRAGMA quick_check` are `ok` on both hosts.
- The exact source archives were removed after success; the immediately prior
  image and release remain for rollback.
- LAN retains about 445 GB free and VPS about 6.6 GB free.

Startup logs contain no V6.3b error. Existing environment warnings remain: LAN
has disabled CSRF, insecure cookies, short audit retention and the paused
`Romarg` SSH credential; VPS has insecure cookies and short audit retention.
