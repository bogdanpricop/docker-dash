# V1.4 Activity Center — rollout results

Data: 2026-07-26

Commit funcțional: `0265a545d099b31b9b83670a38649c899cdb4133`

Branch: `agent/advanced-compose-gitops`

PR: [#13](https://github.com/bogdanpricop/docker-dash/pull/13)

## Validare locală și CI

- targeted engine/route/UI helpers: 3 suites, 18 tests passed;
- full Jest: 187 suites passed, 2.507 tests passed, 4 skipped, 2.511 total;
- ESLint: 0 errors; 13 warnings preexistente în fișiere neatinse de batch;
- `git diff --check`: clean;
- GitHub CI run `30204770118`: success;
- Docker Build & Push run `30204770136`: success.

## Staging `192.168.13.20`

- strict SSH host-key verification: passed;
- pre-deploy: 0 active provider operations, 0 unknown, 0 active snapshot-policy runs;
- release: `/home/localadmin-a/docker-dash/releases/0265a54`;
- imagine: `docker-dash:8.21.4-0265a54`;
- container: healthy, restart count 0;
- DB backup: `/data/backups/pre-deploy-0265a54.db`, 2.743.336.960 bytes;
- config backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-0265a54/.env`;
- backup DB: `integrity_check=ok`, 0 foreign-key errors;
- live DB: `integrity_check=ok`, 0 foreign-key errors;
- unauthenticated operation list/detail/events/cancel: HTTP 401;
- Activity Center asset: noul resolve/cancel UI prezent;
- fatal/runtime log scan: clean.

## Live read-only Activity evidence

DB conținea o operație terminală și nicio operație activă/unknown. Proiecția publică pentru `op_9a29d776855c6467f7803cd923` a confirmat:

- type `system.runtime.smoke`, state `succeeded`;
- owner explicit `system`;
- native task bound, public state `complete`;
- `canCancel=false`, `canResolve=false`;
- raw native ref, request, idempotency hash, lease owner și lock scopes absente din JSON-ul public.

Nu s-a cerut anularea și nu s-a rezolvat artificial nicio operație. Acțiunile rămân pentru canary operațional desemnat, cu provider evidence.

Smoke-ul vizual in-app nu a fost disponibil deoarece nu exista un browser tab controlabil; nu s-a folosit un controller alternativ. Sintaxa asset-ului, helpers, route contract și live asset delivery au fost verificate.

## Public VPS

Deploy-ul pe `89.37.212.66:2224` rămâne blocat fail-closed de host-key mismatch. Fingerprint-ul ED25519 prezentat în această sesiune este `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`, diferit de cheia confirmată din `known_hosts` linia 8. Verificarea nu a fost ocolită și serverul nu a fost modificat.

## Rollback

Rollback-ul folosește imaginea `docker-dash:8.21.4-bbb5784` și config backup-ul de mai sus. Nu există migrare nouă în V1.4. Înainte de rollback se verifică din nou că nu există operații active; orice `unknown` își păstrează lock-ul până la reconciliere manuală bazată pe evidence.
