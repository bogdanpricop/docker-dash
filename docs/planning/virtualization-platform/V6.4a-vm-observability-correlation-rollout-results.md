# V6.4a — VM observability and event correlation rollout

**Release:** v8.53.0 (`1b902c3`) — 2026-07-29

**Backlog:** B206–B215

## Result

V6.4a was committed and pushed on `agent/advanced-compose-gitops`, tagged
`v8.53.0`, and deployed from the immutable Git archive whose SHA-256 is
`386f32a2bd2f4f763d2b749d9a5adf65a5852bd0d5bd7ef5fca386fdde2d9c85`.

Both targets built the Dockerfile production stage and recreated only the
`app` service, with environment and project directory anchored to the
persistent base path. No event, signal rule, topology edge, provider resource
or sidecar was created or changed during rollout.

| Target | Runtime result | Verified pre-deploy backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.53.0-1b902c3`, healthy, 0 restarts; `/api/health` = `8.53.0` | `/home/localadmin-a/docker-dash/backups/v64a-20260729T173201Z-1b902c3/docker-dash.db`, 3,707,191,296 bytes, SHA-256 `21060f8cb35e440eca0d0f8a2cebc27e155f9f791a80806cafec21d4b479e745` |
| VPS `89.37.212.66` | `docker-dash:8.53.0-1b902c3`, healthy, 0 restarts; `/api/health` = `8.53.0` | `/opt/docker-dash/backups/v64a-20260729T173313Z-1b902c3/docker-dash.db`, 435,871,744 bytes, SHA-256 `b60da582b3aaed70f113e4c8e6ff2fb098d2498972fbcf054f787021630c9f42` |

Both online SQLite backups returned `quick_check=ok` and zero foreign-key
violations. Private backup directories retain the pre-deploy environment,
compose coordinates and container inspection evidence.

## Verification

- migration `127_vm_observability_correlation.js` is applied on both databases;
- both targets contain 29 VM metric definitions, 27 governance permissions and
  5 `vm_observability_%` tables;
- external health and the deployed Governance/Observability asset pass on both;
- unauthenticated observability catalog requests return `401` on both;
- startup log scans found no fatal, uncaught, unhandled or migration errors;
- Jest: 262 suites passed, 2,868 tests passed and 4 skipped;
- ESLint: 0 errors and 14 pre-existing warnings.

The LAN clock was approximately 31 seconds behind the archive timestamp and
`tar` reported harmless future-timestamp warnings. Archive checksum validation,
image build and runtime promotion all passed; host time synchronization should
still be checked during routine infrastructure maintenance.

## Safety and rollback

Event adapters only accept caller-supplied observations. Correlation, topology
impact and multi-signal results are local advisory evidence and cannot execute a
provider action. Application rollback uses `docker-dash:8.52.0-2dbbc54` with
the saved environment and release coordinates. Migration 127 is additive;
verified backups are for explicit disaster recovery only and must not be
restored automatically over a live database.
