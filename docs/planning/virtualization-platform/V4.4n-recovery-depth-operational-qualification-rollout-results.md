# V4.4n — Recovery-depth operational qualification rollout results

## Scope

Release v8.87.0 qualifies exactly B137–B146. B137–B138 were already included
in v8.80.0 and B139–B146 in v8.81.0. This release exposes local, host-scoped
schema/runtime evidence while retaining all ten rows as `Partial`; it does not
start provider mutations, network calls or external commands.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,346 tests passed and 4 were skipped (3,350 total);
- 11 focused suites and 46 focused tests passed;
- ESLint completed with zero errors and 14 pre-existing warnings;
- the virtualization registry validated 450 features: 391 Done, 59 Partial,
  0 Open;
- i18n, accessibility, backend/frontend syntax and whitespace gates passed;
- the integrated browser runtime exposed no browser, so visual smoke remains
  explicitly `not_recorded`.

GitHub:

- feature commit `de61d4a9f6cef2b4e60afdda00da9a10b271b288`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.87.0`;
- CI run `30670715949`, tag Docker run `30670715131` and PR Docker run
  `30670715940` all succeeded;
- draft PR `https://github.com/bogdanpricop/docker-dash/pull/13` tracks the
  release branch.

## Deployment evidence

The exact Git tag archive contained 1,888 tracked entries, was 20,121,594 bytes
and had SHA-256
`416235079f8ab358c089f58dbc53b16e7e64e49183eb0041d7f8794f86281e05`.
It contained no `.env`, `.git/` or `node_modules/` entry. Both transferred
archives matched that digest, and extraction left the existing `.env` digest,
owner and mode unchanged.

The application was briefly quiesced on each target while the live SQLite file
was copied. v8.86.0 was restarted and healthy before source extraction/build.
The retained rollback copies are:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.87.0-de61d4a.db` | 2,093,187,072 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.87.0-de61d4a.db` | 454,377,472 | 0600 | `ok` |

Each target built the verified archive with
`docker build --target production --build-arg APP_VERSION=8.87.0` and tagged
`docker-dash:8.87.0`. Only Compose service `app` was force-recreated.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:a070f63c7adc919e3d1cbf17f803000261a5ee4c0306692d52a0a655d18b872c` | `8.87.0`, healthy | 0 |
| VPS | `sha256:e5204e1fc0985304655d47a5b1ccb05901ac6750783e238e9754728acc65aad7` | `8.87.0`, healthy | 0 |

No migration is introduced by v8.87.0. The latest applied migration remains
exactly `169_provider_privileged_compliance.js` (170 recorded migrations on LAN
and 169 on VPS), and live `PRAGMA integrity_check` returns `ok` on both.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Enabled flags | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 (`ESXi-6`) | 10/10 | 0 | 5 | 0 | `5ae62fc675cfacdad0301edd178e346b6ef1eed9263339d239e586e2bb88d238` |
| VPS | explicit synthetic Proxmox host 1; no active virtualization provider exists | 10/10 | 0 | 0 | 0 | `0bbefb26e81f30f01456eb5790d594ba22f255c30d7e07a960d1cb02c7864aa8` |

Both envelopes contain the exact ordered IDs B137–B146, expose the mixed
implementation releases v8.80.0/v8.81.0 and produce stable evidence hashes.
No scoped runtime record exists for either selected provider, so the response
reports zero instead of inferring success. In particular, B138 remains
`not_observed` with zero integrity-detail/evidence records even when backup
flags are enabled.

LAN retains its pre-existing backup policy/execution and restore/drill/DR
flags; restore-replication depth stays false. VPS retains every relevant flag
as false. Qualification only reads and deduplicates these settings. The
foundation and network-backup envelopes remain backward-readable with their
original exact ten IDs, 10/10 schema and stable hashes on both targets.

On both deployments:

- unauthenticated access to the protected endpoint returns 401;
- the served source contains the `recovery-depth` UI marker;
- startup/runtime logs contain zero error-level, uncaught or fatal entries;
- `.env` remains mode 0600 with its prior owner;
- every qualification safety counter is zero;
- the application image is healthy with zero restarts, and internal and
  workstation health requests report v8.87.0.

Temporary local and transferred release archives were removed after
verification. The two rollback database backups were intentionally preserved.
