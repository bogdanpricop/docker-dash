# V4.4o — DR and security operational qualification rollout results

## Scope

Release v8.88.0 qualifies exactly B147–B156. B147–B148 were implemented in
v8.81.0 and B149–B156 in v8.82.0. The release reads scoped DR/security
evidence without starting a provider mutation, network call or external
command, and keeps every external execution/collector gap `Partial`.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,348 tests passed and 4 were skipped (3,352 total);
- 5 focused suites and 32 focused tests passed;
- ESLint completed with zero errors and 14 pre-existing warnings;
- registry validation remained 450 features: 391 Done, 59 Partial, 0 Open;
- i18n, accessibility, syntax and whitespace gates passed;
- the integrated browser runtime exposed no browser, so smoke remains
  explicitly `not_recorded`.

GitHub:

- feature commit `635df3bd310a70c403f0ec8de05dd30329ca0603`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.88.0`;
- CI run `30672522826`, tag Docker run `30672522179` and PR Docker run
  `30672522802` all succeeded;
- draft PR `https://github.com/bogdanpricop/docker-dash/pull/13` tracks the
  release branch.

## Deployment evidence

The exact tag archive contained 1,890 entries, was 20,127,478 bytes and had
SHA-256
`83797964a8266e59b9eaeba354930bc78a2b67b8aff3354e0859d1d96b979068`.
It excluded `.env`, `.git/` and `node_modules/`; both transfers matched the
digest and extraction preserved each `.env` digest, owner and mode.

The application was quiesced only while copying SQLite. v8.87.0 was restarted
healthy before extraction/build. Retained rollback copies:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.88.0-635df3b.db` | 2,105,339,904 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.88.0-635df3b.db` | 454,377,472 | 0600 | `ok` |

Each target built `docker-dash:8.88.0` from the verified archive and only
Compose service `app` was force-recreated.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:98de21b1e04a89d29eb8eeb32cd88a879a288796ab43664144e6dc2f270a4b0e` | `8.88.0`, healthy | 0 |
| VPS | `sha256:c2ec166bc552c0eb6f6ee30789cccce2dd4106e817fe165eb767dd5b68945f69` | `8.88.0`, healthy | 0 |

No migration is introduced. The latest applied migration remains
`169_provider_privileged_compliance.js` (170 recorded rows on LAN and 169 on
VPS); live and backup `PRAGMA integrity_check` return `ok`.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Enabled flags | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 (`ESXi-6`) | 10/10 | 0 | 2 | 0 | `65af3f71f7c65a27ea535520ac71d1e307e7f5dd7ff485bfff4cbd35b85eb39f` |
| VPS | synthetic Proxmox host 1; no active provider exists | 10/10 | 0 | 1 | 0 | `d67d25402696574b2dc782e674c10a621aa46b855ad60f58bbc79ffc7bffaf2d` |

Both envelopes contain exact ordered IDs B147–B156, mixed implementation
releases v8.81.0/v8.82.0 and stable hashes. LAN retains
`DD_PROVIDER_DR_RUNBOOKS=true` and `DD_PROVIDER_SECURITY_ASSURANCE=true`; VPS
retains only security assurance true. Flags are not interpreted as evidence:
B148 failback, B149 DR test, B150 measured objectives and B155 key providers all
remain `not_observed` with zero scoped records.

The foundation, network-backup and recovery-depth batches retain their exact
IDs, 10/10 schema, stable hashes and zero safety counters. On both targets the
protected endpoint returns 401 without a session, runtime logs contain zero
error/fatal/uncaught entries, and internal plus workstation health report
v8.88.0 with zero restarts.

Temporary release archives were removed locally and remotely. Both rollback
database backups were intentionally preserved.
