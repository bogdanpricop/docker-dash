# V3.8 — DR objectives and provider security assurance rollout results

## Scope

The release candidate covers exactly B149–B158. B149/B150 extend the existing
DR protection-group/runbook surface. B151–B158 add versioned, imported or
provider-reported security assurance without releasing a new provider mutation.

## Current result

- DR overview exposes workload RPO/RTO objective counts and non-disruptive test
  readiness with bubble-network, temporary-clone and cleanup boundaries;
- security packs cover Secure Boot, vTPM, encryption, confidential compute and
  hardening evidence without treating missing evidence as compliant;
- key-provider records accept only symbolic secret references and perform no
  health probe;
- confidential VM preflight is persistent, hash-bound, expiring and never
  authorizes VM creation;
- all collection and provider-mutation capabilities remain fail-closed; the
  code default is off and the metadata-only assurance surface was explicitly
  enabled on the two rollout targets.

## Release/deployment evidence

Release-candidate validation on 2026-07-31:

- 310 Jest suites passed;
- 3,317 tests passed and 4 were skipped;
- ESLint completed with zero errors and 14 pre-existing warnings;
- self-service i18n and accessibility gates passed;
- virtualization registry validated 450 features: 391 Done, 59 Partial, 0 Open;
- `git diff --check` passed;
- the known Redis-mock post-test/open-handle warnings remain non-failing;
- local Docker/browser smoke is unavailable in this workstation session.

Release and rollout:

- commit `902bd85c42c30f55cb09121816246dbe30dd1310`, annotated tag
  `v8.82.0` and latest GitHub release published;
- CI run `30632633677` and Docker build runs `30632633629`/`30632633823`
  completed successfully; PR #13 remains draft;
- identical Git archive SHA-256
  `be45a353136fd09fb858a850fbb94a35ee3346a8bca1986876e4daefa187e60b`
  transferred to LAN and VPS (20,045,766 bytes, 1,871 tracked entries);
- production target built as `docker-dash:8.82.0` and
  `docker-dash:8.82.0-902bd85` on both hosts;
- online SQLite backups retained as
  `/data/backups/predeploy-v8.82.0-902bd85.db` (1,864,491,008 bytes on LAN;
  445,526,016 bytes on VPS), mode `0600`, backup integrity `ok`;
- only Compose service `app` was recreated through the canonical compose file;
- both containers are healthy with restart count zero and internal/external
  health endpoints report version `8.82.0`;
- migration `167_provider_security_assurance.js` is applied on both databases,
  all three tables exist and `PRAGMA integrity_check` returns `ok`;
- `DD_PROVIDER_SECURITY_ASSURANCE=true` appears exactly once on each host;
- the UI asset is served and the unauthenticated assurance API returns the
  expected `401`, proving the route remains protected;
- transferred remote archives were removed; rollback database backups were
  preserved.
