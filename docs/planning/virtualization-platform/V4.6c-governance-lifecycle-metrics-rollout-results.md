# V4.6c — Governance lifecycle and VM metrics rollout

**Release:** v8.52.0 (`2dbbc54`) — 2026-07-29

**Backlog:** B196–B205

## Result

V4.6c was committed and pushed on `agent/advanced-compose-gitops`, tagged
`v8.52.0`, and deployed from the immutable Git archive whose SHA-256 is
`efbe96ac4f25ad4241b6849c13794ef892dd1fd640e9fbdbdd226b2c5944eb59`.

Both targets built the Dockerfile production stage and recreated only the
`app` service. Compose used the persistent base directory for `.env` and the
immutable release compose file explicitly. No provider resource, egress
sidecar, credential or tenant was mutated during rollout.

| Target | Runtime result | Verified pre-deploy backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.52.0-2dbbc54`, healthy, 0 restarts; `/api/health` = `8.52.0` | `/home/localadmin-a/docker-dash/backups/v46c-20260729T171305Z-2dbbc54/docker-dash.db`, 3,707,191,296 bytes, SHA-256 `f70b0a3d6be15462752d02d07f15abf8388ca9dd90082c2d6ccd00c12d2cedc6` |
| VPS `89.37.212.66` | `docker-dash:8.52.0-2dbbc54`, healthy, 0 restarts; `/api/health` = `8.52.0` | `/opt/docker-dash/backups/v46c-20260729T171439Z-2dbbc54/docker-dash.db`, 435,871,744 bytes, SHA-256 `7384558eb01becd14d08473c6ba0a8996af287d47d5a2f69944f10a071c62f92` |

Both online SQLite backups independently returned `quick_check=ok` and zero
foreign-key violations. Each private backup directory also contains the
pre-deploy `.env`, active compose file(s), compose coordinates and container
inspection evidence.

## Verification

- migration `126_governance_metrics_foundation.js` is applied on both databases;
- both targets contain 13 canonical VM metric definitions, 25 governance
  permissions and 38 governance/VM-metric tables;
- external health and Governance Lifecycle UI asset checks pass on both;
- unauthenticated lifecycle catalog requests return `401` on both;
- startup log scans found no fatal, uncaught, unhandled or migration errors;
- Jest: 261 suites passed, 2,859 tests passed and 4 skipped;
- ESLint: 0 errors and 14 pre-existing warnings.

## Safety and rollback

Lease expiry only marks cleanup work, access-review revoke preserves evidence,
and tenant deletion requires all blockers plus an export checksum and typed
confirmation. Metric adapters accept caller-supplied observations; rollout did
not claim or initiate live provider collection.

Application rollback uses the retained `docker-dash:8.51.0` image with the
saved environment and release coordinates. Migration 126 is additive; the
verified database backups are retained for explicit disaster recovery and must
not be restored automatically over a live database.
