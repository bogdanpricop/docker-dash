# V4.2c — Shared disk topology rollout results

**Release:** v8.25.0 (`e4eb3bc`) — 2026-07-28

## Validation

- focused topology, storage-posture, page and adapter tests: 23 passed;
- full suite: 240 suites passed, 2,782 tests passed, 4 skipped (2,786 total);
- ESLint: 0 errors; production images built with `docker build --target production`
  on the deployment targets because a local Docker CLI is unavailable.

## Deployment

| Target | Release image | Backup | Result |
|---|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.25.0-e4eb3bc` | `backups/v42c-20260728T155833Z-e4eb3bc/docker-dash.db` SHA-256 `17db5680b4599e861108a22fb755d348476330479658b29a4216fe5e32aff4a8` | healthy |
| VPS `89.37.212.66` | `docker-dash:8.25.0-e4eb3bc` | `backups/v42c-20260728T155917Z-e4eb3bc/docker-dash.db` SHA-256 `675d7f81de0fdeee793dd269d12f1b94f6de2a9459b9ab16c89aa0212e6b5951` | healthy |

Both targets returned `/api/health` version `8.25.0`. The rollout used SSH-key
authentication and deployed only the `app` service with `--no-deps`, preserving
the existing Compose services and release flags. No provider operation or
storage mutation was submitted.

## Deferred live smoke

The topology endpoint requires an authenticated host-view session and a live
provider endpoint with at least two VMs referencing the same backing. No
provider-native backing reference was queried or exported during rollout; the
feature remains a read-only evidence projection.
