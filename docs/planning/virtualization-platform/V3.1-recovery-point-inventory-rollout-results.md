# V3.1 — Recovery-point inventory rollout results

## Published artifact

- functional commit: `3cf80eba55f96e8cc250c54927b7722470e707e9`;
- branch: `agent/advanced-compose-gitops`, draft PR #13;
- CI run `30220671582`: successful;
- Docker build run `30220671566`: successful;
- exact Git archive: 18,882,214 bytes, SHA-256 `a638929e41714dfc72e85f3321462057a1e19ea2174a6dda8c0275e0ee44040d`;
- staging release: `/home/localadmin-a/docker-dash/releases/3cf80eb`, recursively read-only;
- image: `docker-dash:8.21.4-3cf80eb`;
- image ID: `sha256:b1dbabd36e20e7d1e3d23df5ab5b5b6aa4e86a4825175ba0eb216d701dbb590b`;
- OCI revision: exact functional SHA.

## Repository validation

- focused V3.1/provider/client/route/UI tests: 7 suites, 115 passed;
- post-hardening recovery catalog/XO rerun: 2 suites, 29 passed;
- complete local regression: 218 suites, 2,693 passed and 4 explicitly skipped;
- ESLint: zero errors and 13 pre-existing warnings;
- `git diff --check`: clean;
- exact committed SHA passed GitHub syntax, lint, test, i18n and Docker-build workflows.

The in-app browser skill had no attached browser instance in this session. DOM/XSS tests, frontend syntax validation, staging HTML/static-asset requests and API smoke were therefore used as the available UI gates.

## Pre-deploy evidence and backup

- previous container: healthy, restart 0, revision `1b25b7cccb6a55a0306325becd7e4f23159a33bd`;
- filesystem: 478,029,628 KiB available, 51% used;
- backup directory: `/home/localadmin-a/docker-dash/backups/v31-20260726T211759Z-3cf80eb`;
- online SQLite backup: 2,905,509,888 bytes;
- backup SHA-256: `f72cd501963181cf8c75567c93cd36c2528894ed838b635da489fd33f14df6e6`;
- `PRAGMA quick_check`: `ok`;
- foreign-key violations: 0;
- `.env` and `docker-compose.yml` retained mode 0600 beside the database backup.

Read-only verification initially used a read-only bind mount; SQLite could not create/open its auxiliary lock state there. Verification was rerun with the backup directory mounted read-write into a one-shot container. It created an empty WAL and SHM only; the database checksum above was taken before verification and the WAL remained zero bytes. This did not affect the live database.

## Staging deployment

- target: `localadmin-a@192.168.13.20`, strict known-host checking;
- Compose project: `docker-dash` with project directory `/home/localadmin-a/docker-dash` and release-scoped Compose file;
- `APP_VERSION=8.21.4-3cf80eb`;
- `DD_PROVIDER_RECOVERY_POINT_INVENTORY=true`;
- existing HA/placement mutation flags retained unchanged;
- application recreated with `--no-build --no-deps` after the image was built and inspected;
- migration `117_provider_recovery_points.js` applied successfully;
- resulting container: running, healthy, restart count 0, exact expected image and OCI revision.

## Smoke evidence

| Check | Result |
|---|---|
| `/api/health` | HTTP 200 |
| application shell | HTTP 200 |
| `/js/pages/recovery-points.js` | HTTP 200 |
| unauthenticated recovery-point API | HTTP 401 |
| authenticated endpoint 5 (standalone ESXi) | HTTP 400, `PROVIDER_RECOVERY_POINT_UNAVAILABLE` |
| runtime feature flag | `true` |
| migration row | present |
| repository/recovery-point tables | present, empty as expected |
| temporary smoke sessions | 0 after cleanup |
| final container | healthy, restart 0 |

The live staging endpoint is standalone ESXi 8.x and has no configured backup manager. Its typed unsupported response is the correct fail-closed result; no snapshot was misrepresented as a backup. Live XO and PVE/PBS inventory remains pending until such an endpoint is placed in the staging scope.

Two early smoke harness attempts exposed operational-only issues: one bounded `SQLITE_BUSY` while inserting a temporary session and `dotenv` informational output contaminating a captured numeric ID. The final harness used a 30-second busy timeout, bounded retry, last-line capture and explicit cleanup verification. One leaked temporary session from the harness was found, removed and verified absent before the successful final run.

## VPS result

Deployment to `root@89.37.212.66:2224` remains blocked by strict host-key verification. The pinned ED25519 entry in local `known_hosts` is:

`SHA256:pwHZ3Yp3H57gaCuAzDxe1ZtSVwAvxm4txXGXGaDNcVg`

The endpoint presents:

`SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`

No transfer, login, key replacement or bypass was attempted after the strict failure. The presented key must be verified through an independent trusted channel before deployment can continue.

## Rollback point

- prior image: `sha256:986da1dbb35abfdd12e183e0c30f6b86cbd915b91733f18b228249bc61adfcde` (`8.21.4-1b25b7c`);
- prior release: `/home/localadmin-a/docker-dash/releases/1b25b7c`;
- migration 117 is additive and forward-compatible with the prior runtime;
- restore the backed-up `.env`, select the prior image/release and recreate only `app`; database restore is not required for ordinary image rollback.
