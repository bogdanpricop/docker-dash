# V6.7 — Guarded migration factory rollout

**Release:** v8.74.0

**Feature commit:** `121e99c`

**Date:** 2026-07-30

## Outcome

B416–B425 were implemented, tagged `v8.74.0`, pushed to draft PR #13 and
deployed as `docker-dash:8.74.0-121e99c` to LAN and VPS. Migration 148 is
present exactly once on both databases, all ten migration-factory tables are
present, and the governance catalog contains 89 permissions including the two
permissions introduced by this batch.

## Validation before rollout

- Focused validation: 3 suites and 23 tests passed; the dedicated B416–B425
  suite contains 10 tests.
- Full validation: 285 suites passed; 3,109 tests passed and 4 were skipped.
- ESLint: 0 errors and the existing 14 warnings.
- Self-service localization: 11 languages, 19 keys and 209 values passed.
- Accessibility contract: 8 UI and 5 CSS checks passed.
- Fresh database: migration 148 applied once, ten tables created and the two
  new permissions increased the governance catalog to 89 permissions.
- GitHub CI run `30510698690` and Docker Build & Push runs `30510698547` and
  `30510698691` completed successfully for commit
  `121e99c4e36dce376bfbe833afcd766e2ea75307`.
- Interactive browser discovery returned no available browser in this session;
  frontend syntax, UI source contracts and the full repository suite were used
  as the UI gate.

## Artifact

- Exact Git archive:
  `docker-dash-8.74.0-121e99c-20260730061928198.tar.gz`.
- Size: 19,731,170 bytes.
- SHA-256: `afb4ee6a03c38aabe950f883d36df40cba43254722776621098687966869c32c`.
- Both targets recomputed and matched the checksum before extraction/build.
- Both builds used the Dockerfile `production` target and the exact release
  directory. Existing `.env` checksums remained unchanged across deploy.

## Backup and deploy evidence

| Target | Runtime | Backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.74.0-121e99c`, image ID `sha256:c780911753c542c6edffc410fe96507bd29011ee758c7b20476646c737fb5ffb`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v67-20260730T031903Z-121e99c/docker-dash.db`, 3,772,141,568 bytes, mode 600, SHA-256 `3883d9331955754af6ab9bbe62547c8c75185d351b0628264192fdff79562b4d` |
| VPS `89.37.212.66` | image `docker-dash:8.74.0-121e99c`, image ID `sha256:662b572ab89344c4d7b5500b7387849bf78913957334c5924b9efe727f8efef2`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v67-20260730T032305Z-121e99c/docker-dash.db`, 443,531,264 bytes, mode 600, SHA-256 `ad422c1c6b277c59eb22e4ced280706f0b27831ef6bb9391ec2b3aa9b7462e72` |

Each backup was produced with the running application's `better-sqlite3`
online backup API, copied out of the container, copied into a writable `tmpfs`
and checked with `PRAGMA quick_check` in a networkless container with a
read-only root filesystem using the previous release image.

The first VPS preflight used the LAN database path and stopped before archive
extraction, image build or container replacement. Mount inspection established
the VPS database path as `/data/docker-dash.db`; the corrected preflight and
rollout then completed successfully with no runtime impact.

## Post-deploy checks

- LAN and VPS `/api/health` report `status=ok`, version `8.74.0` and standalone
  mode from the external addresses.
- The unauthenticated migration-factory endpoint returns HTTP 401 on both
  targets.
- Migration/table/permission query returns
  `{migration148:1,tables:10,permissions:89,newPermissions:2}` on both targets.
- Remaining free space: 446,509,789,184 bytes on LAN and 4,024,463,360 bytes on
  VPS.
- Immediate rollback is retained as release/image `8.73.0-c49a059` on both
  targets. Exact release/image `8.72.0-75ec438` was removed after validation.
