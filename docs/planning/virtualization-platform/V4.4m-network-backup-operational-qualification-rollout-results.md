# V4.4m — Network and backup operational qualification rollout results

## Scope

Release v8.86.0 qualifies exactly B124, B125 and B129–B136. Their
implementation was already included in v8.80.0. This release removes stale
release-inclusion blockers and exposes local schema/runtime evidence without
promoting any row from `Partial` or invoking a provider executor.

## Release evidence

Validation on 2026-08-01:

- 313 Jest suites passed;
- 3,344 tests passed and 4 were skipped (3,348 total);
- ESLint completed with zero errors and 14 pre-existing warnings;
- self-service i18n and accessibility gates passed;
- the virtualization registry validated 450 features: 391 Done, 59 Partial,
  0 Open;
- backend/frontend syntax and `git diff --check` passed;
- the focused qualification, network, backup, page and registry suites passed;
- the integrated browser runtime exposed an empty browser list, so browser
  smoke remains explicitly `not_recorded`.

GitHub:

- feature commit `84122da20cfb47a58b0f9bacce5292daee70ba24`;
- annotated tag and release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.86.0`;
- CI run `30668709081`, tag Docker run `30668708059` and PR Docker run
  `30668709094` all succeeded;
- draft PR `https://github.com/bogdanpricop/docker-dash/pull/13` tracks the
  release branch.

## Deployment evidence

The Git tag archive contained 1,886 tracked entries, was 20,115,484 bytes and
had SHA-256
`1fe371a42cc00c1d9619cc5a102b726cc67b0a559dcb72fe6f6fb9dc76d38d77`.
It contained no `.env`, `.git/` or `node_modules/` entry. Both transferred
archives matched the same digest, and the existing `.env` digest was unchanged
by extraction.

The application was briefly quiesced on each target while the live SQLite file
was copied. v8.85.0 was restarted and healthy before source extraction/build.
The retained rollback copies are:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.86.0-84122da.db` | 2,086,813,696 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.86.0-84122da.db` | 454,377,472 | 0600 | `ok` |

Each target built the verified release archive with
`docker build --target production` and tagged `docker-dash:8.86.0`. Only
Compose service `app` was force-recreated with `APP_VERSION=8.86.0`.

| Target | Image ID | Internal/external health | Restarts |
|---|---|---|---:|
| LAN | `sha256:bf96520b2c19da55a273fb099b45f0b746572149e7644c2e578c95a5a4c3af5d` | `8.86.0`, healthy | 0 |
| VPS | `sha256:087f1bfd7b664b32d4f6bb44518a192539526b22dc715cb7d5b31d539c075020` | `8.86.0`, healthy | 0 |

No migration is introduced by v8.86.0. On both databases the latest applied
migration remains exactly `169_provider_privileged_compliance.js`, and live
`PRAGMA integrity_check` returns `ok`.

## Qualification evidence

| Target | Provider input | Schema | Runtime observed | Backup flags enabled | Browser | Evidence hash |
|---|---|---:|---:|---:|---:|---|
| LAN | active vSphere host 5 (`ESXi-6`) | 10/10 | 0 | 2 | 0 | `40f251080f7d0e800a94eaa4bdfae4645cbaa177b7ce925d5a19890b47971f7c` |
| VPS | explicit synthetic Proxmox host 1; no active virtualization provider exists | 10/10 | 0 | 0 | 0 | `b2848ea2dcf36023f9e534a56e87750a36baedd288293e84683c7dc83be89dbc` |

Both envelopes contain the exact ordered IDs B124, B125 and B129–B136. No
scoped runtime record exists for either selected provider, so both deployments
report zero rather than infer success. B124 and B125 report persisted
provider/network/external counters as zero.

LAN retains its pre-existing explicit `DD_PROVIDER_BACKUP_POLICIES=true` and
`DD_PROVIDER_BACKUP_EXECUTION=true` settings; VPS retains both defaults as
false. The qualification reads and deduplicates those flags but neither changes
them nor dispatches work. The v8.85 foundation batch remains backward-readable
with its original exact ten IDs and 10/10 schema on both targets.

On both deployments:

- unauthenticated access to the protected batch endpoint returns 401;
- the served source contains the `network-backup` UI/API markers;
- startup/runtime logs contain zero error-level, uncaught or fatal entries;
- `.env` remains mode 0600 with its prior owner;
- the qualification invocation reports zero provider mutations, network calls
  and external commands;
- the application image is healthy with zero restarts and both internal and
  external health endpoints report v8.86.0.

Temporary local and transferred release archives were removed after
verification. The two rollback database backups were intentionally preserved.
