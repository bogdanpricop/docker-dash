# V1.1 Preflight și validation results

**Data:** 26 iulie 2026

**Batch:** V1.1 Common VM detail și Activity Center

**Status:** implementat, publicat și validat live pe staging

## Implementare

- migrarea aditivă 109 și cache persistent numai pentru resursa publică normalizată;
- snapshot upsert atomic cu identity normalization și limită 64 KiB/resursă;
- detail envelope comun `1.0` pentru Proxmox, vSphere și Xen;
- fallback la cache cu freshness `stale` când refresh-ul live eșuează;
- tabs consistente Overview, Hardware, Disks, Network, Snapshots, Tasks și Events;
- action matrix read-only cu blockers capability/state/resource/policy/permission/release;
- host-scoped RBAC, canonical ID-only routing și error sanitization;
- inventar și detail UI unificat cu deep links persistente;
- Activity Center pentru operațiile durabile, filtre și update WebSocket;
- VM search în command palette din SQLite snapshots, fără provider network fan-out;
- navigație comună și link-uri explicite către paginile provider-specific.

## Verificări locale

- 5 suite țintite, 35 teste trecute;
- full suite: 180 suite trecute;
- 2.453 teste trecute, 4 skipped, 2.457 total;
- ESLint: 0 erori, 13 warnings preexistente;
- syntax check pentru API, router și cele două pagini frontend;
- migration 109: create, idempotent reapply, FK cascade și rollback validate;
- cache corrupt/oversized/cross-host este ignorat fără divulgarea resursei;
- fallback offline nu include mesajul brut al providerului;
- toate acțiunile comune conțin `ACTION_NOT_ENABLED` și sunt disabled;
- `git diff --check`: curat.

## Deploy gates

- commit funcțional `549ba98`, push în PR #13; GitHub CI și Docker Build & Push verzi;
- staging rulează `docker-dash:8.21.4-549ba98`, healthy, cu 0 restarturi;
- backup DB consistent: `/data/backups/pre-deploy-549ba98.db` (2,5 GiB);
- backup configurație: `/home/localadmin-a/docker-dash/deploy-backups/env-before-549ba98`;
- release păstrat în `/home/localadmin-a/docker-dash/releases/549ba98`;
- migrarea 109 aplicată la `2026-07-26 11:59:37`, cu `foreign_key_check` fără erori;
- ESXi host 5: 5/5 VM-uri normalizate, fără truncation și fără native-ref leak;
- common detail `1.0`: freshness `fresh`, toate cele 7 secțiuni și action release gate validate;
- snapshot search a găsit VM-ul din cache fără network fan-out;
- inventory/detail/operations/search neautentificate răspund 401;
- asset-urile `virtual-machines.js` și `activity-center.js` sunt servite de imaginea live;
- browser render smoke nu a putut rula: browserul integrat nu este disponibil în această sesiune;
- VPS rămâne blocat: fingerprint-ul ED25519 primit este `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`, diferit de cheia cunoscută; strict checking nu a fost ocolit;
- rollback: imaginea `docker-dash:8.21.4-b795b9a`, backup-ul DB și backup-ul `.env` sunt păstrate.
