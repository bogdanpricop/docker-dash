# V6.6a — Hardware and performance foundation rollout

**Release:** v8.70.0

**Feature commit:** `3b7fb63`

**Date:** 2026-07-30

## Result

B376–B385 were implemented, tagged `v8.70.0`, pushed to draft PR #13 and
deployed as `docker-dash:8.70.0-3b7fb63` to LAN and VPS. Migration 144 is
present exactly once on both databases, with both hardware tables, 82
governance permissions in total and the two new hardware permissions.

The release adds bounded normalized host observations, compatibility tags,
CPU common/extra/missing baselines, a non-executing CPU compatibility policy
editor, NUMA topology and VM fit, CPU pinning conflicts, real-time workload
checks and hugepage/memory-pressure dashboards. There is no provider hardware
apply endpoint.

## Verification

- Full Jest: 281/281 suites passed; 3,069 tests passed and 4 skipped.
- Focused hardware/governance validation: 14/14 tests passed.
- ESLint: 0 errors and 14 pre-existing warnings.
- JavaScript syntax, `git diff --check`, version synchronization and the
  existing localization/accessibility gates passed locally.
- A fresh-database smoke applied migration 144 once and confirmed both tables,
  82 total governance permissions and both new permission keys.
- GitHub CI run `30506231759` and Docker Build & Push run `30506231718`
  completed successfully for the tagged commit.
- Exact Git archive: `docker-dash-8.70.0-3b7fb63.tar.gz`, 19,658,753 bytes,
  SHA-256 `6337cc4743ea213c78efc66053be910324ed0e7b335e637430c02fb7de343003`.

## Safe deployment evidence

Both targets validated the exact archive checksum, retained a mode-600 copy of
the unchanged base `.env`, created an online SQLite backup and passed
`quick_check` by opening it read-only with the previous v8.69.0 image inside a
network-disabled/read-only-root container with tmpfs scratch directories. The
Docker `production` target was built and Compose recreated only `app` using the
base project directory and base `.env`.

| Target | Runtime | Verified backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.70.0-3b7fb63`, image ID `sha256:22ce75534cc26c1d6b902f894715e34ec6aac7b5ac94c715be3bd4635136cce4`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v66a-20260730T014206Z-3b7fb63/docker-dash.db`, 3,772,141,568 bytes, mode 600, SHA-256 `321c2f2f95880341e6f072ddea8d48118404fcd291fc551fb8e51215755c6e5f` |
| VPS `89.37.212.66` | image `docker-dash:8.70.0-3b7fb63`, image ID `sha256:6728f2f6f923862f3698a240ce64bf94d5aa6fbe119004a2711d2afdbca92b54`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v66a-20260730T014329Z-3b7fb63/docker-dash.db`, 443,531,264 bytes, mode 600, SHA-256 `5dbfc73b403ab5944788fdd5353df8a31e245a8c7c0af2f0bbdbd9ecb68de12b` |

The immediate prior release/image `8.69.0-671dc66` remains available on both
targets. The exact previous-previous release/image `8.68.0-819d3e6` and both
incoming archives were removed after verification. Available root filesystem
space after rollout is 439,885,271,040 bytes on LAN and 5,031,702,528 bytes on
VPS.

## Smoke checks

- LAN and VPS `/api/health` report `status=ok`, version `8.70.0` and standalone
  mode; both containers are healthy with zero restarts.
- External `/` returns HTTP 200 on both targets.
- Unauthenticated `/api/governance/lifecycle/hardware` returns HTTP 401 on both
  targets.
- Live databases report migration 144 once, both hardware tables, 82 total
  governance permissions and both new permission keys.

## Rollback

The immediate rollback is `docker-dash:8.69.0-671dc66` with release directory
`v8.69.0-671dc66`. The verified pre-migration backups above are compatible with
that image. Use the base project directory and base `.env` and recreate only
`app` with `APP_VERSION=8.69.0-671dc66`.
