# V0.3d — Automation operations and lifecycle readiness rollout

**Release:** v8.57.0  
**Feature/deployed commit:** `8e19e0e`  
**Date:** 2026-07-29

## Result

V0.3d was pushed to `agent/advanced-compose-gitops`, tagged `v8.57.0` and
deployed from the same tested commit to both standalone environments. The
immutable source archive had SHA-256
`ba1802158be95e202d9cf9ed21cb2af4f33527f4804cb0782789eec92346a648`.

## Pre-deployment verification

- Jest: 266/266 suites passed; 2,909 tests passed and 4 skipped.
- Dedicated B246–B255 verification: 12/12 tests passed.
- ESLint: 0 errors and 14 pre-existing warnings.
- JavaScript syntax checks and `git diff --check` passed.

## Deployment evidence

| Environment | Runtime | Verified online SQLite backup |
|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.57.0-8e19e0e`, healthy, 0 restarts | `/home/localadmin-a/docker-dash/backups/v03d-20260729T192335Z-8e19e0e/docker-dash.db`, 3,737,411,584 bytes, SHA-256 `36978c574306ac220d400659d2966eca1540e049f830057031e760601ec3ca91` |
| VPS `89.37.212.66` | `docker-dash:8.57.0-8e19e0e`, healthy, 0 restarts | `/opt/docker-dash/backups/v03d-20260729T192336Z-8e19e0e/docker-dash.db`, 435,916,800 bytes, SHA-256 `8eac6d5c1d198a6159e5d7760923ebec12d059a646622a8c667b457787d91def` |

Each backup was produced through the online `better-sqlite3` backup API, copied
out of the running container and checked in an isolated temporary container.
Both returned `quick_check=ok` and zero foreign-key violations. Environment,
rendered Compose and container-inspection evidence are stored beside each
database with private permissions.

## Post-deployment checks

- `/api/health` returned version `8.57.0` on LAN and VPS.
- Migration `131_automation_operations_lifecycle_updates.js` is recorded once.
- SQLite `quick_check` is `ok`; governance permissions = 34, all twelve exact
  V0.3d stores exist and five curated templates are seeded.
- External `/` returned HTTP 200; unauthenticated automation and lifecycle
  update endpoints returned HTTP 401 on both environments.
- Deployment-window scans found no monitor-start failure, timer-tick failure,
  migration failure, uncaught exception or fatal entry.
- Both containers retained the five declared socket, data, stacks, certificate
  and egress-policy mounts.
- Source archives were removed after verification; immutable release
  directories and the previous image remain available for rollback.

## Rollback status

No rollback was required. Both activations passed health, restart, immutable
image, migration, permission, exact-table, template-seed and database-integrity
gates on the first attempt.

## Safety boundary retained

Schedule matches create evidence but do not run workflows. Approval decisions
do not start apply. Unsupported dry-run providers stay unsupported rather than
falling back to mutation. Broker APIs return no secret values. Catalog ingestion
installs no packages, and upgrade advice/prechecks start no upgrade.
