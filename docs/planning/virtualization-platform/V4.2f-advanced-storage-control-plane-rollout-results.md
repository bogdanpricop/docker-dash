# V4.2f — Advanced storage control plane rollout

**Release:** v8.78.0

**Feature commit:** `62b80fc33109b19748734ff991b054cfd2d0a82a`

**Date:** 2026-07-30

## Outcome

B082–B084, B087–B089, B091–B095 and B097–B100 are implemented and marked
`Done`. Eleven new persistent contracts cover format conversion, policy,
latency, path, cleanup, QoS, tiering, object-storage, suite-health and general
storage-change evidence. B091 and B094 explicitly reuse the already guarded
native consolidation and shared-disk topology flows. The research catalog now
contains 296 `Done`, 74 baseline `Now` and 80 open `Next`/`Later` rows.

## Validation before rollout

- Focused storage/governance validation: 2 suites and 20 tests passed.
- Final full validation: 288 suites passed; 3,158 tests passed and 4 were
  skipped (3,162 total).
- ESLint: 0 errors and the existing 14 warnings.
- Self-service localization: 11 languages, 19 keys and 209 values passed.
- Accessibility contract: 8 UI and 5 CSS checks passed.
- A fresh in-memory SQLite database applied migration 151 and contained all
  11 new tables plus 98 governance permissions.
- GitHub CI run `30514811685` and Docker Build & Push runs `30514811463` and
  `30514811607` completed successfully for the exact feature commit.
- Interactive browser discovery returned no available browser in this session;
  frontend syntax, UI source-contract tests and external HTTP checks were used
  as the browser-independent gate.

## Artifact

- Exact Git archive: `docker-dash-8.78.0-62b80fc.tar.gz`.
- Size: 19,798,186 bytes.
- SHA-256:
  `0e86973f5999b19bbc05ece61ffc7926b07497a9f2525d3948b3eb58209156a8`.
- Both targets recomputed and matched the checksum before extraction.
- Both builds used the Dockerfile `production` target and OCI version label
  `8.78.0-62b80fc`. Existing `.env` checksums were unchanged.

## Backup and deploy evidence

| Target | Runtime | Backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.78.0-62b80fc`, image ID `sha256:6d0198eaca935f8ac9cd06eb693a9f4e8b68e95c6386f3367a0813d6b516716a`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v42f-20260730T045212Z-62b80fc/docker-dash.db`, 3,692,023,808 bytes, mode 600, SHA-256 `b68a8238ecb5475901683bf2965ac9664dce8a33c5803d0477c481626de1ddb4` |
| VPS `89.37.212.66` | image `docker-dash:8.78.0-62b80fc`, image ID `sha256:1d90c4a73c17354bab05104b49fe567ee0daa8c1b195c1c21fc41a8f57d59a84`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v42f-20260730T045419Z-62b80fc/docker-dash.db`, 429,625,344 bytes, mode 600, SHA-256 `507ff9e59d4917593b00fe45d6cba0f75033e638cfba503deb8c59db83cf84d2` |

Each backup was created through the running application's SQLite online backup
API. It was copied into writable tmpfs and validated with
`integrity_check=ok` using the previous image in a networkless container with a
read-only root filesystem (6 GiB tmpfs on LAN, 1 GiB on VPS).

## Deployment notes and post-checks

- The first LAN attempt stopped before backup and build because a manifest
  check called `node` on a host where Node is intentionally absent. The
  rollback trap retained the healthy v8.77 container. The incomplete release
  directory was path-validated, removed and recreated; the retry used a
  host-independent manifest check and completed successfully.
- External `/api/health` calls return HTTP 200, `status=ok`, version `8.78.0`
  and standalone mode from both addresses.
- The unauthenticated Advanced Storage route returns HTTP 401 on both targets,
  confirming route registration and authentication enforcement.
- The live databases contain all 11 storage tables and 98 permissions.
- Remaining free space is 431,130,435,584 bytes on LAN and 1,617,813,504 bytes
  on VPS.
- Immediate rollback is retained as release/image `8.77.0-5be7828` on both
  targets. The exact v8.76 release directories and image tags were removed only
  after successful validation; their source remains recoverable from Git tag
  `v8.76.0`.
