# V1.3.1 Preflight results — Common VM snapshot lifecycle

Data: 2026-07-26  
Commit funcțional: `c245c46`

## Verificare locală și CI

- 185 suite Jest trecute;
- 2.496 teste trecute, 4 skipped, 2.500 total;
- snapshot-focused final: 3 suite, 27 teste trecute;
- ESLint: 0 erori, 13 warnings preexistente;
- `git diff --check`: clean;
- GitHub CI run `30203203161`: success;
- Docker Build & Push run `30203203162`: success;
- browser render smoke indisponibil: sesiunea nu a expus nicio instanță in-app Browser; nu a fost înlocuit cu alt controller.

## Staging `192.168.13.20`

- release: `/home/localadmin-a/docker-dash/releases/c245c46`;
- image: `docker-dash:8.21.4-c245c46`;
- container: healthy, 0 restarts;
- DB backup consistent: `/data/backups/pre-deploy-c245c46.db`, 2.739.187.712 bytes;
- backup `integrity_check=ok`, `foreign_key_check=0`;
- env backup: `/home/localadmin-a/docker-dash/deploy-backups/env-before-c245c46/.env`;
- migrarea `110_provider_vm_snapshots.js` aplicată;
- DB live post-migration: `integrity_check=ok`, `foreign_key_check=0`;
- `DD_PROVIDER_VM_SNAPSHOTS=true`, count 32 și chain depth 16;
- handler-ele `vm.power` și `vm.snapshot` înregistrate;
- health, asset frontend și scan fatal/migration errors: OK;
- inventory, preflight și submit fără autentificare: toate `401`.

## ESXi live read-only

- endpoint host ID 5, 5 VM-uri observate;
- VM verificată: ID canonic `ddr_vm_*`, identitate `stable`, state `running`;
- adapterul a emis `snapshot` și `snapshotQuiesced` pe baza capability/tools evidence;
- inventar snapshot live: 0 elemente, native refs expuse: false;
- maximum count 32, maximum depth 16, observed depth 0;
- create crash-consistent preflight: allowed, zero blockers, `NOT_A_BACKUP` warning;
- plan hash SHA-256 de 64 caractere și confirmare explicită;
- `protection.isBackup=false`;
- nicio create/revert/delete mutation nu a fost trimisă: nu există o VM disposable desemnată.

## VPS public

Deploy-ul nu a fost executat. Strict host-key checking a respins din nou conexiunea la `89.37.212.66:2224`: endpoint-ul prezintă ED25519 `SHA256:tqbf5XHb2ZnCHekfiHbBEQPdUGSYAOM4UHOTtlfLU7g`, diferit de cheia cunoscută local (offending entry `known_hosts:8`). Cheia nu a fost acceptată și `known_hosts` nu a fost modificat.

## Rollback

Rollback-ul aplicației folosește imaginea anterioară `docker-dash:8.21.4-b593c59` și env backup-ul de mai sus. Migrarea 110 este aditivă și poate rămâne pentru un rollback de aplicație; rollback-ul complet al bazei folosește backup-ul consistent `pre-deploy-c245c46.db`. Înainte de rollback se verifică operațiile `running`, `reconciling` sau `unknown`; smoke-ul read-only nu a creat operații.
