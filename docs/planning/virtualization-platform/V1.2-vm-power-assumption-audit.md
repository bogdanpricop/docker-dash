# V1.2 Assumption audit — VM power

| Ipoteză | Risc | Control |
|---|---|---|
| un HTTP 2xx înseamnă state final | task încă rulează | native task poll + state verification |
| reboot poate fi retrimis | dublu reboot | handler non-idempotent, reconciliation-only după submit |
| capability provider implică allowed per VM | state/tools/HA pot bloca | per-resource allowed actions și live preflight |
| force are același risc ca shutdown | pierdere/corupție date | typed name, warning și audit forced=true |
| canonical ID poate fi trimis providerului | providerul cere ref nativ | identity resolution server-side, encrypted native refs |
| bulk poate crea pe rând | partial submission | toate preflight-urile trec, apoi create într-o tranzacție |
| raw Xen start este portabil | domeniul poate să nu aibă config persistent | start rămâne unsupported pe raw |
| guest shutdown este task peste tot | vSphere guest calls sunt sincrone | state reconciliation fără retransmitere |
| operator role implică orice host | privilege expansion | `requireHostAccess('operate')` per endpoint |
| B031 aparține power batch-ului | create fără template/storage/network plan | remapat explicit la V1.6 |
