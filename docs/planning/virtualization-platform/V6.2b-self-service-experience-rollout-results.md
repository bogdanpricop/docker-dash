# V6.2b — Self-service experience rollout

**Release:** v8.69.0

**Feature commit:** `671dc66`

**Date:** 2026-07-30

## Result

B366–B375 were implemented, tagged `v8.69.0`, pushed to draft PR #13 and
deployed as `docker-dash:8.69.0-671dc66` to LAN and VPS. Migration 143 is
present exactly once on both databases, with all six new experience tables,
80 governance permissions in total and the four new portal/feedback
permissions.

The release completes the self-service group with approval-bound temporary
quota grants, inherited safe branding, contextual help, redacted guided
troubleshooting, explainable advisory recommendations, keyboard VM controls,
a non-destructive mobile incident view, localization/accessibility CI gates and
local-only product-feedback aggregates after explicit opt-in.

## Verification

- Full Jest: 280/280 suites passed; 3,058 tests passed and 4 skipped.
- Focused self-service/telemetry/portal validation: 37/37 tests passed.
- ESLint: 0 errors and 14 pre-existing warnings.
- JavaScript syntax checks, `git diff --check`, version synchronization, the
  11-language/209-value localization gate and the 13-check accessibility gate
  passed locally.
- A fresh-database smoke applied migration 143 once and confirmed six tables,
  six seeded help topics, 80 total permissions and four new permissions.
- GitHub CI run `30505072119` and Docker Build & Push run `30505072135`
  completed successfully for the tagged feature commit.
- Exact Git archive: `docker-dash-8.69.0-671dc66.tar.gz`, 19,639,538 bytes,
  SHA-256 `f47e9bb966f32e17238d6088a589f5b54caf902756eec7894c0e1f323795428b`.

## Safe deployment evidence

Both targets validated the exact archive checksum, retained a mode-600 copy of
the unchanged base `.env`, created an online SQLite backup and opened that
backup read-only with the previous v8.68.0 image in a network-disabled,
read-only-root container with writable tmpfs scratch directories. The
application was built with the Docker `production` target and Compose recreated
only `app` using the base project directory and base `.env`.

The first LAN backup-validator invocation used a read-only parent mount and
could not open SQLite. It stopped before extraction/build/recreation; the same
backup then passed `quick_check` after `/backup` was mounted as tmpfs while the
database file remained a nested read-only bind mount.

| Target | Runtime | Verified backup |
|---|---|---|
| LAN `192.168.13.20` | image `docker-dash:8.69.0-671dc66`, image ID `sha256:b28ba18a59a84faa0cb785150b7fcc36dbfc0c92b736eee4c5cfe2423d28c74f`, healthy, 0 restarts, 5 mounts | `/home/localadmin-a/docker-dash/backups/v62b-20260730T011911Z-671dc66/docker-dash.db`, 3,772,141,568 bytes, mode 600, SHA-256 `097bfc9586d66bea0a8c5e9d610287c6412fc8fad0a8bf0a41aa6ea102081f6c` |
| VPS `89.37.212.66` | image `docker-dash:8.69.0-671dc66`, image ID `sha256:9c881d45838dc0867ecd27653d6beee26df083ef34c968d7b9a60183f933f95f`, healthy, 0 restarts, 5 mounts | `/opt/docker-dash/backups/v62b-20260730T012156Z-671dc66/docker-dash.db`, 443,531,264 bytes, mode 600, SHA-256 `0b8320e9e2670a90144d0a964c20ba25127a050386952497373184cad2707e15` |

The immediate prior release/image `8.68.0-819d3e6` remains available on both
targets. The exact previous-previous release/image `8.67.0-0f486ad` and both
incoming archives were removed after verification. Available root filesystem
space after rollout is 443,726,848,000 bytes on LAN and 5,825,032,192 bytes on
VPS.

## Smoke checks

- LAN and VPS `/api/health` report `status=ok`, version `8.69.0` and standalone
  mode; both containers remain healthy with zero restarts.
- External `/` returns HTTP 200 on both targets.
- Unauthenticated branding, incident and feedback-preference endpoints return
  HTTP 401 on both targets.
- Live databases report migration 143 once, six experience tables, 80 total
  governance permissions and all four new permission keys.

## Rollback

The immediate rollback is `docker-dash:8.68.0-819d3e6` with release directory
`v8.68.0-819d3e6`. The verified pre-migration backups above are compatible with
that image. Use the base project directory and base `.env` and recreate only
`app` with `APP_VERSION=8.68.0-819d3e6`.
