# V3.9 — Provider security lifecycle rollout results

## Scope

Release v8.83.0 covers exactly B159–B168: virtual-hardware baseline,
insecure-protocol evidence, certificate trust/rotation projection, exact
official-advisory correlation, exposure priority, expiring exceptions,
dry-run/low-risk remediation contracts and secret-reference enforcement.

## Current result

- closed-schema evidence covers virtual hardware, transport, certificate trust
  and exposure without an implicit scan;
- exact version/build matching creates bounded findings from the existing
  official catalog; range-like or absent metadata remains unknown;
- exceptions require owner/reason/expiry/compensating controls;
- remediation plans require dry-run, dependencies and verified rollback;
- low-risk execution remains separately disabled and no production adapter is
  registered;
- certificate ownership/latest renewal state is projected without PEM;
- invalid manifest/job/template credential fields return 422 and the submitted
  document/reference values are not persisted.

All provider-native collectors and production remediation/certificate adapters
remain fail-closed, so B159–B168 remain `Partial` honestly.

## Release evidence

Validation on 2026-07-31:

- 311 Jest suites passed;
- 3,326 tests passed and 4 were skipped (3,330 total);
- ESLint completed with zero errors and 14 pre-existing warnings;
- self-service i18n and accessibility gates passed;
- virtualization registry validated 450 features: 391 Done, 59 Partial,
  0 Open;
- frontend/backend syntax and `git diff --check` passed;
- the known Redis-mock post-test/open-handle warnings remain non-failing;
- local Docker/browser smoke remained unavailable on this workstation.

GitHub:

- feature commit `2a45db435cf16bb7cf78768e9d3a29991e7915ba`;
- annotated tag `v8.83.0` and latest release
  `https://github.com/bogdanpricop/docker-dash/releases/tag/v8.83.0`;
- CI run `30634947629`, PR Docker run `30634947738` and tag Docker run
  `30635136440` all succeeded;
- draft PR #13 remains open and was updated to the v8.83.0 scope.

## Deployment evidence

The Git tag archive contained 1,875 tracked entries, was 20,069,767 bytes and
had SHA-256
`c46bf633674aa39715f64030bb047d9d41e1c8e8d8444dfa55d5d34b9d664379`.
It contained no `.env`, `.git` or `node_modules` entry. Both transfers matched
the same digest.

Online SQLite backups were created before source extraction and retained in the
application data volume:

| Target | Backup | Bytes | Mode | Integrity |
|---|---|---:|---:|---|
| LAN `192.168.13.20` | `/data/backups/predeploy-v8.83.0-2a45db4.db` | 1,881,915,392 | 0600 | `ok` |
| VPS `89.37.212.66` | `/data/backups/predeploy-v8.83.0-2a45db4.db` | 445,526,016 | 0600 | `ok` |

Each host built the release with `docker build --target production` and tags
`docker-dash:8.83.0` plus `docker-dash:8.83.0-2a45db4`. Only Compose service
`app` was force-recreated with `APP_VERSION=8.83.0`.

| Target | Image ID | Health | Migration 168 |
|---|---|---|---|
| LAN | `sha256:00a2406b029d1f4d618969d63a457d603591f4dd085683efba8306fb034988bd` | internal/external 8.83.0, healthy, 0 restarts | 2026-07-31 13:40:20 |
| VPS | `sha256:dc1dbf2c175268918137fbbf45e0e247a657db142ebe9284efb1a332a0c936fd` | internal/external 8.83.0, healthy, 0 restarts | 2026-07-31 13:38:24 |

All five migration tables exist on both databases with zero initial rows and
post-migration `PRAGMA integrity_check=ok`. The protected lifecycle API returns
401 without authentication, and the served UI contains the new security
lifecycle marker.

`DD_PROVIDER_SECURITY_LIFECYCLE=true` and
`DD_PROVIDER_SECURITY_LOW_RISK_REMEDIATION=false` each occur exactly once per
target. Both `.env` files remain mode 0600. No error-level startup log was
observed (0 on LAN and 0 on VPS); the two matching loader entries also report
`errorCount:0`.

The LAN checkout is root-owned/read-only. Direct extraction therefore failed
before `.env` or container recreation. The rollout was resumed through a
one-shot UID-0 container with only the checkout and release archive mounted;
the first Compose read then stopped on root-owned mode-0600 `.env` while the old
healthy container stayed running. Ownership was restored to `localadmin-a`, the
new image was recreated successfully, and no application volume was mounted in
the extraction container.

Transferred remote archives and the local temporary archive were removed after
verification. The two rollback database backups were intentionally preserved.
