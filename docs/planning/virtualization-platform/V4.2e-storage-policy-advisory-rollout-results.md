# V4.2e — Storage policy advisory rollout results

**Release:** v8.27.0 (`e361c6f`) — 2026-07-28

## Validation

- focused policy-advisory, storage-posture, placement and route tests: 55 passed;
- full suite: 242 suites passed, 2,790 tests passed, 4 skipped (2,794 total);
- ESLint: 0 errors; production images were built with `docker build --target production`
  on deployment targets because a local Docker CLI is unavailable.

## Deployment

| Target | Release image | Backup | Result |
|---|---|---|---|
| LAN `192.168.13.20` | `docker-dash:8.27.0-e361c6f` | `backups/v42e-20260728T165455Z-e361c6f/docker-dash.db` SHA-256 `2437aaeccc2238cd4db627d56365605a2a7392bd4389c86994baa858afc1b83e` | healthy |
| VPS `89.37.212.66` | `docker-dash:8.27.0-e361c6f` | `backups/v42e-20260728T165543Z-e361c6f/docker-dash.db` SHA-256 `81bfa2907cbb565d8fbdfd3dd1f9386a905fb07d916784d22ad96818df6aad22` | healthy |

Both targets returned `/api/health` version `8.27.0`. The SSH-key rollout rebuilt
only the application image and used Compose `--no-deps`, preserving existing
services and release flags. No storage policy was persisted, reserved or applied
to a provider, and no storage mutation was submitted.

## Deferred live smoke

The policy advisory requires an authenticated host-view session and a reachable
provider endpoint. It was not used to change any provider storage setting during
rollout; a future operation must still perform its own authorized fresh preflight.
